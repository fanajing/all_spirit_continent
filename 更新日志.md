# 全魂大陆 All Spirit Continent · 更新日志

> 觉醒武魂，猎杀魂兽，凝练魂环，感应魂技——在 Minecraft 中走完一条属于你自己的斗罗修炼之路。
>
> 适用版本：Minecraft 1.21.1 · NeoForge 21.1.29+ · 作者：梵阿景

---

## 0.6-beta（最新）

**AI 推演质量加固 · 云端安全 · 异常防御 · 跨 Mod 资源扫描**

### AI 推演质量（基于「魂技生成引擎」规范）

- **温度分级机制**：`ApiClient` 引入 `DEFAULT_TEMPERATURE = 0.7` 与 `clampTemperature()` 夹紧到 `[0, 0.9]`；候选生成 0.7、事实型子任务 0.3~0.5、严禁 0.9+，避免发散幻觉；
- **System Prompt 强化**：
  - 新增「`raw_value` 必须落在环位提示范围」反推公式，按环位 `[年龄倍率]` 自动换算；
  - 武魂真身（9 环）硬约束段落，杜绝 9 环出现低阶原语组合；
  - 范例参数范围对齐：5 环（value 16~32 / cooldown 160~540）与 9 环（value 76~140 / cooldown 800~2400）示例全部落在区间内；
- **EXECUTE 双语义常量化**：`SkillData` 新增 `EXECUTE_KILL_THRESHOLD_MULTIPLIER=2.0`、`EXECUTE_KILL_DAMAGE_MULTIPLIER=4.0`、`EXECUTE_NORMAL_DAMAGE_MULTIPLIER=1.0`，斩杀/普通伤害分支不再依赖魔法数字；
- **9 环禁列原语扩展**：`SUMMON_ENTITY / CROP_GROW / FOOD_BLESS / HARVEST / BONEMEAL` 五类生产/养成原语在 9 环禁用，规避无趣的真身技能组合。

### 云端安全

- **上传开关上线**：`ModConfig.ENABLE_CLOUD_UPLOAD`（默认 `true`）；`CloudSyncService.flush / queueSkill / queueStatsUpdate` 三处加守门，仅开关开启时才会上传；
- **AI 超时收紧**：`AI_TIMEOUT_SECONDS` 由 30 秒下调至 10 秒，注释明确「最小 10 秒」，避免长尾请求拖慢体验；
- **`/douluo status` 实时显示**「云端上传：开 / 关」状态，方便排查；
- 多语言（zh_cn / en_us）同步新增 `status_cloud_upload` 文案。

### 异常防御与黑名单

- **`SkillAnomalyDetector`**：4 类异常规则——数值越界、原语与环位冲突、原语组合互斥、依赖 Mod 缺失；命中即标记为「异常」并不进入缓存；
- **`SkillBlacklist`**：本地持久化黑名单（JSON），支持按 `skillId` 与 `sourceMod` 双向封禁；
- **拉取/上传双守门**：`CloudSyncService.pullIncremental` 跳过云端黑名单条目，`flush` 跳过本端异常条目，云端异常样本回流反馈给评分系统；
- **`/douluo blacklist` 命令**：查看 / 手动添加 / 清除黑名单，便于社区治理。

### AI 语义分类 fallback

- **`ResourceResolver` 接入 AI 语义分类**：本地词典未命中的实体名降级到 `ApiClient`（温度 `0.3`，事实型任务）做实体类型判定（攻击型 / 防御型 / 召唤型等），结果写入本地缓存，避免重复请求；
- 缓存在玩家维度切换或重启后保留，命中率持续累积。

### 跨 Mod 资源扫描

- **`ModResourceScanner`**：启动时扫描所有已加载 Mod 的 `data/<modid>/skills/` 目录，自动识别 `*.json` 魂技条目并入本地公共库；
- 兼容 Forge / NeoForge / 第三方 Mod 的数据包格式，魂技可跨 Mod 复用；
- 启动事件注册扫描钩子，新装 Mod 进入游戏即可生效，无需重启客户端。

### 工程

- `./gradlew build` 通过，产出 `all_spirit_continent-0.6-beta.jar`；
- 所有改动遵循 0.5 版 API 风格，新增类与方法均纳入既有包路径，零破坏性变更。

---

## 0.5-beta

**武魂参战 · 昊天锤实装**

- **武魂模式**：按 `N` 键（或用调试棒）开启武魂，物品栏将切换为武魂形态——主手发放**昊天锤**，关闭武魂后原物品栏完整归还；死亡/登出自动安全还原，多层防丢失、防复制保护；
- **昊天锤**：攻击力随魂环年限加成成长，主手持有生效；附带**不可破坏**属性，永不会损坏；
- **武魂介绍面板**：锤子上将展示完整的 RPG 魂器信息——品级、魂师等级与封号、攻击力/攻速、**魂环之力**（每一环的魂技名、魂兽名、年限）、武魂绑定与锁定提示，一目了然；
- **融合被动系统**：部分魂技自带「融合被动」效果（如攻击强化、持续治疗、护盾、反伤、净化、加速等），手持武魂武器时被动常驻生效，持续强化自身；
- **跨维度等级/魂环保持**：往返主世界与暮色森林等维度，等级与魂环数据完整保留，属性自动重算；
- 刷怪笼魂兽化：部分结构（如暮色森林 Boss）刷出的怪物同样会魂兽化，击杀可掉落魂环。

---

## 0.4-beta

**对外公开的正式测试版**

- 首个向玩家开放下载的正式测试版本，包含以下全部内容；
- 共享阵营玩家**免配置**即可使用 AI 魂技推演与公共魂技库（作者内置服务）。

---

## 0.3-beta

**联网功能开箱即用**

- **作者服务内置**：AI 推演、公共魂技库同步所需的密钥已安全内置，共享阵营玩家无需任何配置即可联网使用；
- 水晶球新增**合成配方**（钻石 + 紫水晶碎片 + 紫水晶块），不再只能创造模式获取；
- **刷怪笼魂兽化**：地牢、遗迹、暮色森林等刷怪笼刷出的生物同样会魂兽化；
- 登录后补满生命，重生后 HUD 显示修复；
- AI 推演时能更准确地识别来自其他 Mod 的生物。

---

## 0.2-beta

**魂技系统上线（核心特色）**

- **公共魂技库 + AI 即时推演**：吸收魂环时优先从全服魂师共享的魂技库中抽取；没有的组合由 AI 根据「你的武魂 × 魂兽 × 年限」现场推演，生成**独一无二**的专属魂技；
- **六大系别 · 60+ 技能原语**：强攻、控制、防御、恢复、辅助、特效自由组合，涵盖爆发、连击、护盾、反伤、召唤、瞬移、爆炸、粒子特效等炫酷表现；
- **三池分流体系**：臻品 / 品鉴 / 凡品三个品质池，高分魂技更容易被抽到，社区质量自治；
- **评分体系**：用 `/douluo rate` 为魂技打分（1~5 星），24 小时内不可重复评分，差评自动降级沉淀；
- **双阵营模式**：`共享`（加入全球公共库，自动上传分享）或 `独狼`（使用私人 API Key，魂技仅归自己）；
- **依赖 Mod 过滤**：魂技可标记所需 Mod（如暮色森林、冰与火之歌），只有安装了对应 Mod 的玩家才会遇到它们；
- **吸收确认界面**：全新 GUI——展示技能名、效果描述、数值预览、来源与评分，支持「吸收 / 拒绝 / 自行推演」，吸收后顺手评分；
- **魂技释放**：专属快捷键（R 键释放、G 键切换），享受魂环年限加成的威力增幅，自带冷却与精神力消耗。

---

## 0.1-beta

**初版 · 魂师成长体系奠基**

- **武魂觉醒**：使用水晶球觉醒武魂，开启等级修炼之路；
- **完整境界体系**：魂士 → 魂师 → 大魂师 → 魂尊 → 魂宗 → 魂王 → 魂帝 → 魂圣 → 魂斗罗 → **封号斗罗** → 超级斗罗 → 极限斗罗（99 级）；
- **自定义封号**：达到 91 级可用 `/fh <封号>` 为自己封号；
- **魂兽化系统**：游戏内敌对生物自动魂兽化——体型、攻击、血量随年限等比放大，脚底魂环、头顶年限名号，猎杀掉落魂环；
- **魂环六档年限**：十年 / 百年 / 千年 / 万年 / 十万年 / 百万年，年限越高加成越强；
- **经验修炼**：吸收魂环获取经验，每 10 级一个境界瓶颈，**吸收魂环方可突破**继续升级；
- **魂环吸收确认**：吸收前弹出确认窗口，蹲下右键魂环则可直接化为经验；
- **成就系统**：「全魂大陆」成就页，从「觉醒武魂」到「极限斗罗」的完整修炼旅程；
- **HUD 信息**：实时显示等级、称号、精神力与魂环状态；魂帝境界解锁**精神力飞行**；
- 魂视附魔（头盔专属）、调试棒（属性查看 / 等级调节等开发调试工具）。

---

## 温馨提示

- AI 魂技推演需要联网：共享阵营使用作者服务（免费），独狼需自行配置 API Key；
- 「神祇」境界（100 级）尚未开放，敬请期待后续版本；
- 更多玩法与内容正在开发中，欢迎提出建议与反馈。