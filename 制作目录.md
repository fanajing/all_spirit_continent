# 全魂大陆 模组制作目录

> 本文件是项目的**制作目录（开发总账）**，用于防止上下文丢失。
> - 每次开发新内容时，从 **编号 1** 开始记录，逐条追加。
> - 每条记录必须包含：**明细**（做了什么、改了哪些文件、关键代码位置）+ **注意事项**（踩坑、约定、遗留问题）。
> - 更新后请同步更新本文档底部的「当前进度」。

---

# 前言（截至 2026-08-14 的全部历史内容）

## 一、项目基础信息

| 项目 | 内容 |
|---|---|
| 项目名称 | 全魂大陆 (All Spirit Continent) |
| 项目路径 | `C:\Users\admin\All Spirit Continent` |
| 游戏版本 | Minecraft **1.21.1** |
| 模组加载器 | **NeoForge 21.1.29**（FML 4.0.24，NeoForm 20240808.144430） |
| Java 版本 | **JDK 21**（本地：`C:\Program Files\Eclipse Adoptium\jdk-21.0.11.10-hotspot`；IDE 用 `C:\Users\admin\.jdks\ms-21.0.11`） |
| 构建系统 | Gradle 8.8 + `net.neoforged.moddev` v2.0.142 |
| 映射 | Parchment v2024.11.17（**已在 build.gradle 中注释**，避免版本不匹配风险） |
| 模组 ID | `all_spirit_continent` |
| 包名 | `org.fanajing.all_spirit_continent` |
| mod_version | 0.1-beta |
| 模组作者 | 梵阿景 |
| 构建内存 | `org.gradle.jvmargs=-Xmx2G`（gradle.properties） |
| 运行目录 | `run\`（配置、存档、日志） |
| 启动命令 | `.\gradlew.bat runClient`（PowerShell 下，**不支持 && 连接命令**） |

### 标准目录结构（大型模组模块化）

```
src\main\java\org\fanajing\all_spirit_continent\
├── All_spirit_continent.java   主类（注册全部组件 + 登录/克隆同步逻辑）
├── block\ModBlocks.java        方块注册
├── client\                     客户端专属（HUD、输入处理、世界渲染）
│   ├── LevelHudOverlay.java    等级HUD面板
│   ├── VanillaHudHider.java    原版UI隐藏
│   ├── DebugStickInputHandler.java  滚轮切换调试棒模式
│   └── SoulRingRenderer.java   魂环脚底旋转渲染
├── entity\ModEntities.java     实体注册（预留：creature/、monster/）
├── init\
│   ├── ModAttachments.java     附件数据注册（PlayerLevelData）
│   └── ModCreativeTabs.java    创造标签页「全魂大陆」
├── item\
│   ├── ModItems.java           物品注册
│   ├── ShuiJingQiuItem.java    水晶球（蓄力碎裂）
│   └── TiaoShiBangItem.java    调试棒（模式NBT + 右键功能）
├── network\
│   ├── ModNetwork.java         网络包注册中心
│   ├── PlayerLevelSyncPayload.java  S2C 等级数据同步
│   └── SetDebugModePayload.java     C2S 调试棒模式同步
├── util\
│   ├── PlayerLevelData.java    等级系统数据类
│   └── DebugStickMode.java     调试棒模式枚举
└── world\                      世界生成（预留）

src\main\resources\assets\all_spirit_continent\
├── lang\zh_cn.json / en_us.json   双语语言文件（必须同步更新）
├── models\item\                   物品模型 JSON
├── textures\item\                 物品贴图（标准：64×64 PNG，透明背景）
└── textures\entity\               实体/特效贴图（魂环在此）
```

## 二、已完成功能清单

### 1. 模组骨架（最小可运行化）
- 主类仅保留必要结构：`@Mod` 构造器、`commonSetup`、`ServerStarting` 欢迎日志
- 玩家登录时（`PlayerLoggedInEvent`）在聊天栏输出彩色欢迎信息
- 已删除模板冗余（Config.java、示例代码），注释掉 gameTestServer 与 parchment 配置

### 2. 水晶球物品（shuijingqiu）
- 注册：`ModItems.SHUIJINGQIU`，64 堆叠，UNCOMMON 稀有度，创造标签页图标
- **交互**：右键长按蓄力 **2 秒（40 ticks）**，弓拉弦动画 + 紫色 ENCHANT 粒子环绕
- **碎裂**：玻璃碎裂音效 + 30 个 CRIT + 15 个 END_ROD 粒子，消耗 1 个
- **关键作用**：碎裂时激活等级系统（`data.setActivated(true)`）
- 贴图源文件：`C:\Users\admin\Downloads\shuijingqiu.png`（1254×1254 → 缩放至 64×64，100% 透明背景、居中直径约 48px、紫色魔法质感）
- 实现文件：`ShuiJingQiuItem.java`

### 3. 调试棒物品（tiaoshibang）
- 注册：`ModItems.TIAOSHI_BANG`，不可堆叠，EPIC 稀有度，模型复用原版木棍贴图
- **定位**：模组核心调试工具，后续功能均需**手持调试棒**触发（统一交互约定）
- **模式机制**：手持调试棒 + **潜行(Shift) + 滚轮** 切换模式（动作栏提示），模式保存在物品 CUSTOM_DATA NBT 的 `DebugMode` 键
- 模式枚举：`DebugStickMode`（NONE / SOUL_RING），后期在此扩展新模式

### 4. 等级系统（PlayerLevelData + HUD）
- 数据类：`PlayerLevelData`（activated 激活状态、level 等级、spiritPower/maxSpiritPower 精神力），挂载于玩家 Attachment
- **激活方式**：水晶球碎裂后自动激活，聊天提示「等级系统已激活！」
- **持久化**：Codec 序列化 + `copyOnDeath`（死亡保留），UUID 绑定存档
- **HUD 面板**（`LevelHudOverlay`）：左上角（x=10,y=10）半透明黑底面板，**无标题**，显示 5 行：等级、血量(百分比)、饱食度、精神力、护甲
- **UI 互斥**（`VanillaHudHider`）：激活后隐藏原版血条/护甲条/饱食度条，仅在激活状态下生效
- 同步时机：登录时 + 玩家克隆时（`PlayerEvent.Clone`，死亡重生已由 copyOnDeath 处理）

### 5. 魂环系统（本期核心）
- **等级关联规则**：魂环数 = 等级 ÷ 10，**每 10 级一个魂环，最高 10 个 = 100 级**
- **获取方式**：调试棒切到《获取魂环》模式后右键 → 等级 +10（上限 100）→ 同步客户端 → 升级音效 + 脚底一圈 END_ROD/ENCHANT 粒子 + 聊天提示当前魂环数
- **渲染**（`SoulRingRenderer`，纯客户端）：`RenderLevelStageEvent.AFTER_ENTITIES` 阶段，`RenderType.entityTranslucentEmissive`（自发光全亮）平铺 quad 于玩家脚底（Y+0.05 避免 z-fighting）
- **旋转交替**：第 1 环顺时针 → 第 2 环逆时针 → 第 3 环顺时针……（代码中偶数索引取负角、奇数取正角，3°/tick ≈ 6 秒一圈）
- **半径递增**：0.55 + 0.09×索引，叠加不重叠；各环微抬高 0.002×索引 防 z-fighting
- 贴图：`textures\entity\soul_ring.png`（2048×2048，源文件 `C:\Users\admin\Desktop\Soul_Ring.png`）
- **局限**：当前仅渲染本机玩家自己的魂环（客户端只持有自己的等级数据），多人互见需广播所有玩家等级

## 三、核心数据流

```
【服务端权威】
右键调试棒(获取魂环模式)
  └→ TiaoShiBangItem.use()（服务端）
      └→ level += 10（上限100）
      └→ syncLevelDataToClient() → PlayerLevelSyncPayload (S2C)

【客户端】
收到 S2C → 写入本地 Attachment → HUD 面板 + SoulRingRenderer 读取渲染

【模式切换】
客户端滚轮(Shift+手持调试棒) → 本地 NBT 立即写入(即时反馈)
  └→ SetDebugModePayload (C2S) → 服务端 NBT 写入(权威)
```

## 四、开发约定

1. **调试棒是功能启用前置条件**：新功能需要玩家手持调试棒才能触发（例外：魂兽属性查看面板可用「魂视」附魔头盔触发）
2. **双语语言文件必须同步**：zh_cn.json 与 en_us.json 每次一起改
3. **物品贴图规范**：PNG、64×64、Alpha 透明背景、存放 `textures\item\`
4. **特效/实体贴图**：存放 `textures\entity\`
5. **模式类功能**：先加 `DebugStickMode` 枚举值 → 再在 `TiaoShiBangItem.use()` 的 switch 中处理 → 最后补语言文件 `mode.all_spirit_continent.<id>`
6. **等级相关功能**：统一从 `PlayerLevelData` 读取，服务端修改后必须调用 `All_spirit_continent.syncLevelDataToClient(player)` 同步
7. 网络包：C2S 用 `registrar.playToServer`，S2C 用 `registrar.playToClient`，在 `ModNetwork.register` 中注册

## 五、踩坑记录（务必遵守）

1. **JDK 必须 21**：用 Java 25 会报 `Unsupported class file major version 69`；PowerShell 中若 java 版本不对需 `$env:JAVA_HOME = "C:\Program Files\Eclipse Adoptium\jdk-21.0.11.10-hotspot"` 后再运行 gradlew
2. **PowerShell 不支持 `&&`**：多条命令分开执行
3. **NeoForge 21.1.29 不支持 `AttachmentType.syncWith()`**：必须手动网络包同步（PlayerLevelSyncPayload 方案）
4. **1.21.1 的 `Item.getUseDuration` 签名**：`int getUseDuration(ItemStack stack, LivingEntity entity)`，双参
5. **Parchment 版本必须匹配官方发布版本**：不匹配会导致构建失败，当前已注释
6. **ItemStack NBT 操作 API 已变**：`getOrCreateTag()/setTag()` 已移除，改用 `stack.getOrDefault(DataComponents.CUSTOM_DATA, CustomData.EMPTY).copyTag()` 读、`stack.update(DataComponents.CUSTOM_DATA, CustomData.EMPTY, data -> data.update(tag -> ...))` 写
7. **滚轮事件 API**：`InputEvent.MouseScrollingEvent.getScrollDelta()` 已拆分为 `getScrollDeltaX()/getScrollDeltaY()`
8. **§ 颜色码在日志中乱码**：日志用纯文本，游戏内用 `ChatFormatting` + `Component`
9. **gameTestServer 配置**：无测试时会导致启动崩溃，已注释
10. **纹理尺寸**：尽量 2 的幂次方（魂环贴图 2048×2048 合规）；普通物品贴图 64×64
11. **魂环贴图源文件**在桌面 `C:\Users\admin\Desktop\Soul_Ring.png`，已复制入项目，替换贴图时直接覆盖 `textures\entity\soul_ring.png` 即可

## 六、当前进度（2026-08-14）

- ✅ 模组骨架、水晶球、调试棒、等级系统 + HUD、魂环系统全部完成
- ✅ `gradlew build` 与 `runClient` 均验证通过，游戏内待实测操作手感
- ⏳ 待验证/可调项：魂环旋转方向（若与预期相反，改 `SoulRingRenderer` 中角度符号）、旋转速度（SPIN_SPEED=3.0）、半径（BASE_RADIUS=0.55 / RADIUS_STEP=0.09）
- 📋 后期规划：多个不同魂环效果（随等级解锁）、多人互见、世界生成、怪物/生物模块

---

# 正文（从 1 开始编号）

## 1. 修复魂环渲染导致崩溃（2026-08-14）

**明细**：
- 问题：获取魂环后客户端直接崩溃，报错 `IllegalStateException: Missing elements in vertex: UV1, Normal`
- 根因：`RenderType.entityTranslucentEmissive` 的顶点格式要求每个顶点必须包含 UV1（Overlay）与 Normal 两个元素，原 `SoulRingRenderer.drawFlatQuad` 只设置了 UV/Color/Light
- 修复：每个顶点补齐 `.setOverlay(OverlayTexture.NO_OVERLAY).setNormal(0, 1, 0)`，新增 import `net.minecraft.client.renderer.texture.OverlayTexture`
- 验证：重新 runClient 后连续获取魂环至 10/10，无崩溃，聊天栏正常提示魂环数

**注意事项**：
- 以后凡是直接向 `VertexConsumer` 写顶点（自定义世界渲染），必须先确认所用 RenderType 的顶点格式，按需补齐 setUv/setOverlay/setNormal/setLight/setColor
- 崩溃排查方法：先看 `run\logs\latest.log` 末尾的堆栈（crash-reports 可能为空），本次堆栈直接指向 SoulRingRenderer.drawFlatQuad 第 104 行

## 2. 调试棒新增移除魂环模式 + 加大魂环间隔（2026-08-14）

**明细**：
- `DebugStickMode` 新增枚举值 `REMOVE_SOUL_RING("remove_soul_ring")`，滚轮可切换到《移除魂环》模式
- `TiaoShiBangItem` 新增 `removeSoulRing()`：右键等级 -10（最低保持1级），无魂环时提示「没有魂环可移除」；玻璃碎裂音效 + 在最外层魂环半径处生成 POOF 烟雾粒子（"从最大魂环往内减少"由魂环数递减天然实现，魂环数=等级/10）
- `SoulRingRenderer` 的 `RADIUS_STEP` 从 0.09 加大到 **0.15**（环间间隔明显增大，最外层半径约 0.55+9×0.15=1.9）
- 语言文件新增：`mode.all_spirit_continent.remove_soul_ring`、`msg.all_spirit_continent.soul_ring_remove`、`msg.all_spirit_continent.soul_ring_none`（中英同步）
- 构建验证通过（build SUCCESSFUL）

**注意事项**：
- 移除魂环的粒子半径公式（0.55 + (n-1)×0.15）与 `SoulRingRenderer` 的 BASE_RADIUS/RADIUS_STEP **硬编码同步**，以后改渲染常量必须同时改 `TiaoShiBangItem.removeSoulRing` 里的 outerRadius
- 等级下限为 1（`Math.max(1, level-10)`），不会降到 0；魂环数 0 时移除操作被拦截并提示

## 3. 魂环间距 ×5（2026-08-14）

**明细**：
- `SoulRingRenderer.RADIUS_STEP` 从 0.15 增大到 **0.75**（×5），最外层半径约 0.55+9×0.75=7.3 格
- 同步修改 `TiaoShiBangItem.removeSoulRing` 中移除粒子的 outerRadius 公式为 0.55 + (n-1)×0.75（保持与渲染常量一致）
- 编译验证通过

**注意事项**：
- 间距过大时魂环会覆盖玩家周围大片地面，贴图在远处可能超出视线/阴影范围；后续如需调整可直接改 RADIUS_STEP，并同步改 TiaoShiBangItem 中硬编码的 0.75

## 4. 一环放大 + 间隔逐级变大（2026-08-14）

**明细**：
- 新建公共布局类 `util/SoulRingLayout`：`BASE_RADIUS=1.10`（第1环，比旧2环1.30略小）、`STEP_BASE=0.75`、`STEP_GROWTH=0.10`，间隔公式：第i与i+1环间隔 = 0.75 + 0.10×i（逐级变大防重叠）
- `SoulRingRenderer` 删除本地 BASE_RADIUS/RADIUS_STEP 常量，改用 `SoulRingLayout.getRadius(i)`
- `TiaoShiBangItem.removeSoulRing` 粒子半径改用 `SoulRingLayout.getRadius(ringCountBefore-1)`，从此渲染与粒子共用同一公式，彻底消除硬编码不同步隐患
- 编译验证通过

**注意事项**：
- 以后调整魂环半径/间隔只改 `SoulRingLayout` 三个常量即可，渲染与粒子自动同步
- 当前布局：第1环1.10，间隔依次 0.75/0.85/0.95/…/1.55，第10环半径约 1.10+10.35=11.45 格

## 5. 调试棒「调节环大小」模式 + 配方导出（2026-08-14）

**明细**：
- `DebugStickMode` 新增 `RESIZE_RING("resize_ring")`，Shift+滚轮可切入
- `SoulRingLayout` 新增临时调节覆盖：静态 `overrideMaxRadius`（null=默认布局），新增 `getScaledRadius(index, totalRings)`：设置了覆盖值时等比缩放所有环，使最外层恰好等于调节值；调节范围 0.5~50 格
- `DebugStickInputHandler` 扩展：非潜行滚轮（RESIZE_RING 模式下）每档 ±0.1 格调最大环半径（动作栏实时提示）；首次切入该模式时用当前实际最外层半径初始化；新增 ClientTickEvent.Post：手持调试棒 + RESIZE_RING 模式 + Alt + 长按右键 1.5 秒（30 tick）→ 导出配方 JSON 到桌面 `~/Desktop/soul_ring_recipe.json`（满配 10 环为基准，含 maxRingRadius 和 radii 数组，两位小数）
- `SoulRingRenderer` 魂环半径改用 `getScaledRadius`；RESIZE_RING 模式下额外渲染半透明白色预览环（alpha 150，略高 0.03 防 z-fighting，无魂环也能预览）；drawFlatQuad 增加 alpha 参数重载
- `TiaoShiBangItem.removeSoulRing` 粒子半径改用 `getScaledRadius`
- 语言键新增 4 组（中英同步）；编译通过

**注意事项**：
- 配方文件固定写到 `user.home/Desktop/soul_ring_recipe.json`，用户导出后发给开发者，开发者按 radii 数组重新设定 SoulRingLayout 常量
- `overrideMaxRadius` 为 JVM 静态字段：单人集成服务端/客户端同 JVM 共享可正常工作；将来多人（独立服务端）需要把调节值走网络同步，且要存到玩家各自的持久化数据中
- 导出只触发一次需松开右键重置（exported 标志）；长按期间右键会持续触发 use()，RESIZE_RING 模式在 TiaoShiBangItem.use 中无 case 分支，无副作用

## 6. 修正：调节只作用于最大环（2026-08-14）

**明细**：
- `SoulRingLayout.getScaledRadius` 由「整体等比缩放」改为「仅最外层环使用调节值」：`overrideMaxRadius != null && index == totalRings - 1` 时返回调节值，内层各环保持默认布局不变（只调节当前等级最大的魂环）
- 渲染、预览环、移除粒子、导出配方均通过 getScaledRadius 自动适配，无需改动其他代码；导出时 radii 数组 = 前 9 环默认 + 第 10 环调节值
- 编译验证通过

**注意事项**：
- 若调节后的最大环比内层环还小，会出现视觉重叠/穿插，这是用户主动调节导致的正常现象（用户靠预览环自行把控）
- 若在环数非 10 时调节，导出配方仍以满配 10 环为基准，调节值只写入第 10 环（用户意图：设定「最大环」大小）

## 7. 每环独立调节 + 导出全部环位置（2026-08-14）

**明细**：
- `SoulRingLayout` 单一覆盖值 `overrideMaxRadius` 改为**每环独立覆盖数组** `RING_OVERRIDES[10]`（Float[]，null=用默认布局），新增 `getOverride(index)`/`setOverride(index, value)`；`getScaledRadius(index)` 签名单参化（有覆盖用覆盖值，否则默认）
- `DebugStickInputHandler` 滚轮调节目标改为**当前最外层环**（`index = max(ringCount-1, 0)`，即新获取的魂环；无魂环时调第 1 环预览）：每次只写该环覆盖值，旧环调节值保留；动作栏提示改为「第 N 环半径：X 格」
- 移除首次进入 RESIZE_RING 模式的初始化逻辑（每环无覆盖时自然回退默认值）
- 导出配方：radii 数组每环输出最终半径（覆盖优先），maxRingRadius = 第 10 环最终值，即导出全部魂环位置
- 渲染器/预览环/移除粒子调用点同步改单参 getScaledRadius；语言键 ring_size 改为两个参数（中英同步）；编译通过

**注意事项**：
- 调节值只存于内存（JVM 静态数组），重启游戏后全部丢失——导出配方后由开发者写入 SoulRingLayout 常量才算正式设定
- 各环调节互不影响；新增魂环后直接滚轮调新环即可，已调过的环保持不动

## 8. 按导出配方正式设定魂环半径表（2026-08-14）

**明细**：
- 读取用户导出的 `soul_ring_recipe.json`（maxRingRadius=30.75，10 环满配）
- `SoulRingLayout` 删除旧的 BASE_RADIUS/STEP_BASE/STEP_GROWTH 公式，改为**每环精确半径表** `DEFAULT_RADII[10]`：
  1.10 / 1.85 / 2.70 / 3.85 / 5.60 / 7.95 / 11.10 / 15.95 / 21.90 / 30.75
- `getRadius(index)` 改为查表（越界取最近有效值）；调节覆盖机制保留（仍可继续微调再导出）；删除重复的 MAX_RINGS 定义
- 编译验证通过

**注意事项**：
- 后续再改默认半径：直接改 `DEFAULT_RADII` 数组即可（一处生效，渲染/粒子自动同步）
- 第 10 环半径 30.75 格非常大（直径 60+ 格），注意远处大环的渲染视野范围与地面遮挡；如果用户后续觉得过大，可再次导出新配方替换数组

## 9. 新增《开武魂》模式：魂环常隐藏 + 依次释放动画（2026-08-14）

**明细**：
- 魂环改为**常隐藏**：不再随获取魂环显示，只有开武魂后才显示
- 调试棒新增《开武魂》模式（KAI_WU_HUN，模式枚举第 5 个）
- 右键触发链路：服务端检查魂环数（无魂环拦截提示）→ `OpenSoulAnimationPayload` S2C → 客户端 `SoulRingRenderer.startAnimation()` 记录动画开始时间与魂环数
- 动画设计（每个环依次、非同时）：
  - 单环时长 24 tick（1.2 秒），相邻环错开 12 tick（0.6 秒），第 1 环从第 0 秒开始
  - 高度：从头部（1.6 格）线性下移到脚底（0.05 格）
  - 尺寸：前环目标大小 → 峰值（目标×1.2，比目标大一点点）→ 目标尺寸；峰值位于进度 60% 处（即“到脚下之前膨胀到峰值，再缩小回目标”）
  - 第 1 环起始尺寸 0.1（无前环）；动画完成后保持脚底目标尺寸持续旋转
  - 旋转方向交替、旋转速度不变；动画期间插值用 partialTick 平滑
- 无魂环时开武魂：红色提示；有魂环：信标音效 + 紫色提示「武魂开启！魂环数：N/10」
- 渲染器重构：主 pose 只平移到玩家位置，每环自行控制高度（动画期间各环高度不同）
- 《调节环大小》预览环保留（未开武魂时也能看到白色预览环）
- 编译验证通过（sendToPlayer 需强转 ServerPlayer）

**注意事项**：
- 动画状态只存客户端静态字段：重进世界后魂环恢复隐藏（需重新开武魂）
- 开武魂后再获取/移除魂环：显示数 = min(开武魂数, 当前等级数)，获取的新环不自动播动画（除非再点一次开武魂）
- 第 10 环动画峰值半径 36.9 格，动画期间大环直径可达 70+ 格，如果卡顿可减小 PEAK_FACTOR 或 RING_ANIM_TICKS

## 10. 开武魂动画提速：5 秒内全部出完（2026-08-14）

**明细**：
- 用户反馈：武魂出现太慢（原来 10 环共 6.6 秒），不要一个一个出，只要不是一次性全出即可，5 秒内出完
- `RING_ANIM_DELAY` 12 → 8 tick（错开间隔 0.6s → 0.4s，保持先后顺序但紧凑）
- `RING_ANIM_TICKS` 24 → 20 tick（单环动画 1.2s → 1.0s）
- 10 环总时长：20 + 9×8 = 92 tick ≈ 4.6 秒，满足 5 秒内要求
- 编译验证通过

**注意事项**：
- 这两个常量就是动画节奏开关：想让“一波出”更紧凑就减小 DELAY，想让单环动作更快就减小 TICKS
- 环与环只错开 0.4 秒，视觉上接近连续释放；若用户觉得仍偏“同时”，可微调回 8~10 tick 之间

## 11. 开武魂接入按键绑定：N 键开关 + 模组专属分类（2026-08-14）

**明细**：
- 新建 `ModKeyMappings`（客户端）：在按键绑定界面新开「全魂大陆」专属分类（语言键 key.categories.all_spirit_continent），后续所有快捷键都放这里
- 新增《开/关武魂》按键：默认 N 键（InputConstants.KEY_N），可在原版按键绑定界面修改为任意键；通过 RegisterKeyMappingsEvent 注册
- 按键触发链路：ClientTickEvent.Post 里 `consumeClick()` → 客户端根据 `SoulRingRenderer.isMartialSoulOpen()` 判断开关方向 → 发 `KaiWuHunPayload(boolean)` C2S → 服务端 `openMartialSoul`/`closeMartialSoul`
- `TiaoShiBangItem` 的开武魂逻辑重构为 `public static openMartialSoul(level, player)` + 新增 `closeMartialSoul(level, player)`：右键《开武魂》模式与 N 键共用同一套逻辑（环数校验/动画包/音效/提示）
- 关闭链路：`OpenSoulAnimationPayload(0)` → 客户端 `stopAnimation()` 隐藏魂环；关闭音效 BEACON_DEACTIVATE + 灰色提示「武魂关闭！」
- 开武魂音效 BEACON_POWER_SELECT + 紫色提示；无魂环红色提示（不变）
- 按键无需手持调试棒（全局有效）
- 编译验证通过

**注意事项**：
- 武魂开关状态存在客户端静态字段（isMartialSoulOpen），服务端只管校验与转发；重进世界自动复位为关闭
- 后续新增快捷键：在 ModKeyMappings 加 KeyMapping + RegisterKeyMappingsEvent 注册 + 语言键（分类 key.categories.all_spirit_continent 已就绪）
- 快速连按 N 时多个 KaiWuHunPayload 依次到达服务端，开/关顺序正确（客户端状态即时翻转）

## 12. 动画系统化：动画选项模式 + 动画2 脚底膨胀（2026-08-14）

**明细**：
- 现有开武魂动画正式定为**动画1（头部释放下移）**；新增**动画2（脚底膨胀依次出现）**
- 新建 `SoulRingAnimConfig`（util 纯数据类）：当前动画类型 id 静态存储（0=动画1，1=动画2），next/prev 循环切换，显示名翻译键生成；仅存内存重启丢失（单人共享 JVM）
- 调试棒新增《动画选项》模式（SOUL_ANIM，枚举第 6 个）：非潜行滚轮循环切换动画1↔动画2，金色提示「开武魂动画：动画2（脚底膨胀）」
- `OpenSoulAnimationPayload` 扩展为 `(ringCount, animType)`：服务端开武魂时读取 `SoulRingAnimConfig.getCurrentAnimId()` 下发；关闭时发 (0, 0)
- 动画2 规则（渲染器 animType==1 分支）：
  - 所有环都在脚底（不做高度动画），仅尺寸变化
  - 每环：小尺寸 0.1 → 峰值（目标×1.2，50% 进度处）→ 目标尺寸
  - 第 i 环在第 i-1 环到达峰值时刻开始（开始间隔 = 单环时长×峰值进度 = 16×0.5 = 8 tick）
  - 10 环总时长 = 9×8 + 16 = 88 tick ≈ 4.4 秒
- 按键 N 与调试棒右键开武魂都自动使用当前选中的动画类型
- 编译验证通过

**注意事项**：
- 后续新增动画3/4：`SoulRingAnimConfig.ANIM_COUNT` +1 → 渲染器加分支 → 语言键 soul_anim_type_N
- 动画切换即时生效：正在播放的动画不受影响，下一次开武魂使用新动画
- 动画2 峰值进度 0.5 是「前一环到峰值时下一环开始」的关键参数

## 13. 魂环年限体系：6 档颜色贴图 + 调试棒《年限修改》（2026-08-14）

**明细**：
- 用户导入了 6 张魂环贴图（textures/entity/）：十年白/百年黄/千年紫/万年黑/十万年红/百万年金，对应斗罗年限体系
- 新建 `SoulRingAge`（util）：6 档位常量 + 贴图路径映射 + 显示名翻译键；全部魂环默认十年（档位 0）
- `PlayerLevelData` 新增 `ringAge` 字段：CODEC 用 `optionalFieldOf("ringAge", 0)` 兼容旧存档；STREAM_CODEC、全参构造同步扩展；copyOnDeath 走 CODEC 自动保留
- `PlayerLevelSyncPayload` 加 ringAge 字段，登录/变更时同步客户端
- 调试棒新增第 7 个模式《年限修改》（RING_AGE）：右键年限 +1 档（十年→百年→千年→万年→十万年→百万年→回到十年），全部魂环统一换色，同步后立即生效；音效经验拾取 + 青色提示「魂环年限：xxx」
- 渲染器两处（魂环 + 调节预览环）改用 `SoulRingAge.getTexture(data.getRingAge())` 动态选贴图，删除固定 soul_ring.png
- 编译验证通过

**注意事项**：
- 贴图文件名已统一改为全小写（soul_ring_decade.png 等）：ResourceLocation 路径只允许小写字母数字，大写会直接崩溃（ResourceLocationException）
- 年限是全局统一的（所有魂环同色），每环独立年限后续可改为数组（PlayerLevelData 存 int[10] + 循环选择贴图）
- 旧存档无 ringAge 字段自动按 0（十年）处理；新增年限档位只需在 SoulRingAge 加贴图文件名 + 语言键 ring_age_N
- 调试棒右键年限修改不检查是否已激活/有魂环（纯调色，后续正式获取魂环时可绑定年限校验）

## 14. 年限改为每环独立：只修改最大魂环（2026-08-15）

**明细**：
- 用户反馈：不要一次性修改全部魂环，要求像「调节环大小」一样只修改最大（最外层）魂环
- `PlayerLevelData`：`int ringAge` 改为 `List<Integer> ringAges`（每环独立年限档位，index=环序号，默认全十年）
  - CODEC 用 `Codec.INT.listOf().optionalFieldOf("ringAges", List.of())` 兼容旧存档（旧 ringAge 字段自动忽略，默认十年）
  - getRingAge(int index)/setRingAge(int index, int age)（越界返回十年/自动扩展列表）；getRingAges/setRingAges 列表整体读写（网络同步用）
  - STREAM_CODEC 固定写 10 个 VarInt
- `PlayerLevelSyncPayload`：ringAge 改 `List<Integer> ringAges`（ByteBufCodecs.collection + VAR_INT）
- 调试棒《年限修改》右键：目标环 = 当前最外层环（ringCount-1，无魂环时第 1 环），年限 +1 循环；提示改为双参数「第 X 环年限：xxx」
- `SoulRingRenderer`：主渲染循环改为每环独立创建 RenderType（按各自年限选贴图）并逐个 endBatch；调节预览环用对应环年限贴图
- 编译验证通过

**注意事项**：
- 每环年限独立后，各环可混合颜色（如内环十年白色、外环万年黑色），与「调节环大小」机制对称
- 旧存档中旧 ringAge 字段不会被读取（新结构默认全十年）；若需迁移旧数据可读 optionalFieldOf 旧键
- 每环独立 RenderType 每次渲染创建新实例（10 环开销可忽略）；如后续性能敏感可缓存同色环合批
## 15. 修复「升到 5 环不显示」——武魂开启时环数取实时等级（2026-08-15）

**明细**：
- **原因**：渲染环数原来取 `min(animRingCount, level/10)`，`animRingCount` 是上次开武魂时固定的环数（4），之后用调试棒获得第 5 环时武魂还开着，`min(4,5)=4` 导致新环不显示
- **修复**：武魂开启状态下渲染环数直接取实时等级 `min(level/10, MAX_RINGS)`，新环立即以最终位置出现（不重播入场动画），移除魂环也立即消失
- **同步加固**：`PlayerLevelSyncPayload` 编解码改为手动实现（固定写 10 个环年限 VarInt，顺序 activated/level/spiritPower/maxSpiritPower/ringAges×10），不再用 ByteBufCodecs.collection，避免集合 codec 潜在不兼容
- 编译验证通过

**注意事项**：
- 动画进行中获取新魂环会直接出现在最终位置（无入场动画），这是预期行为
- `animRingCount` 仍保留用于判断武魂开/关状态；移除魂环后武魂开着会立即减少显示的环数

## 16. 称号系统 + 等级上限改 99（2026-08-15）

### 16.1 封号格式调整：xx斗罗（超级）/ xx斗罗（极限）
- 用户反馈：不要叫「xx超级斗罗」「xx极限斗罗」，改为「xx斗罗（超级）」「xx斗罗（极限）」
- TitleSystem 改为：封号 + 「斗罗」+ 后缀标记（super_mark/limit_mark 语言键），无封号仍显示「超级斗罗」「极限斗罗」；91-94 仍为「xx斗罗」不变

**明细**：
- 新增 `TitleSystem` 工具类：境界划分 1-10魂士 / 11-20魂师 / 21-30大魂师 / 31-40魂尊 / 41-50魂宗 / 51-60魂王 / 61-70魂帝 / 71-80魂圣 / 81-90魂斗罗 / 91-94封号斗罗 / 95-98超级斗罗 / 99极限斗罗 / 100神祇（未完成）
- 自定义封号：`PlayerLevelData` 新增 `title` 字段（CODEC `optionalFieldOf("title", "")` 兼容旧档 + STREAM_CODEC writeUtf + 同步 payload STRING_UTF8）
  - 91-94：封号斗罗，有封号则 xx斗罗（`/fh 唐昊` → 唐昊斗罗）
  - 95-98：有封号则 xx超级斗罗；99：有封号则 xx极限斗罗（封号跨境界保留）
- 新增 `/fh` 指令（RegisterCommandsEvent 注册，greedyString 参数）：
  - `/fh xx` 设置封号（去空格后 1-2 个字，需≥91级，否则拒绝）
  - `/fh clear` 清除封号（回到「封号斗罗」）；`/fh` 无参查看用法与当前封号
  - **踩坑**：1.21.1 中 StringArgumentType 在 `com.mojang.brigadier.arguments` 包（不是 net.minecraft.commands.arguments）
- 达到 91 级弹窗：服务端 grantSoulRing 检测跨入 91 级发金色提示；客户端 LevelHudOverlay 每帧检测 `level≥91 && title空` 时弹出聊天输入框（预填 `/fh `，会话内只弹一次）+ 屏幕大字「自定义封号」
- HUD：称号行显示在等级上方（金色 0xFFD700），面板行数 5→6
- **等级上限 99、环数上限 9**：TiaoShiBangItem 的 MAX_LEVEL 改 99、MAX_RINGS 改 9（神祇 100 级未开放）；提示文案 %s/10 → %s/9，soul_ring_full 改「魂环已达上限（9个，99级）！」
- 编译验证通过

**注意事项**：
- SoulRingLayout.MAX_RINGS 保持 10（布局表 10 环不删），但渲染/获取均按 9 环限制；第 10 环布局保留备用
- 100 级神祇：TitleSystem 已定义但当前版本无法达到（等级上限 99）；后续开放神祇只需改 MAX_LEVEL 并补充 100 级内容
- 封号弹窗会话内只弹一次（静态标志），玩家 Esc 关闭后想再设可手动输 /fh；重启客户端若仍无封号会再弹一次

## 17. 魂兽系统：原版生物魂兽化 + 年限/体型/攻击力/掉落（2026-08-15）

**明细**：
- 新增文件（服务端）：`util/SoulBeastAge`（年限体系：概率表/档位/显示格式/体型与攻击加成计算）、`util/SoulBeastData`（实体附件，只存年限 int）、`world/SoulBeastWorldData`（SavedData：百万年计数+冷却）、`world/SoulBeastSpawnHandler`（全部事件处理）
- 新增文件（客户端）：`client/SoulBeastClientCache`（entityId+UUID → 年限缓存）、`client/SoulBeastRingRenderer`（脚底魂环 + 头顶年限文字）、`client/SoulRingItemProperties`（物品贴图 ring_tier override）
- 新增文件（其他）：`network/SoulBeastSyncPayload`、`item/SoulRingItem`（魂环掉落物）；`ModAttachments` 加 SOUL_BEAST 附件（serialize 持久化）、`ModNetwork`/`ModItems`/`ModCreativeTabs` 注册
- **年限规则**：1 年 ~ 9999 万年（MAX_AGE=99_990_000）；权重概率表（敌对：十年45%/百年25%/千年15%/万年8%/十万年5%/百万年1.5%/千万年0.4%/9999万年0.1%；无攻击手段被动生物：1~10 万年，十年50%…十万年5%）；敌我判定 = 有无 ATTACK_DAMAGE 属性
- **新手保护**：世界出生点 5000 格内十万年及以上自动降级（CAPPED_TABLE 重新随机）
- **百万年限制**：全世界最高 2 只（SavedData 挂主世界 DataStorage 全维度共享）；生成时 canSpawnMillion 校验超限降级十万年档；死亡后 onMillionDeath 计数-1 + 冷却 30 分钟（COOLDOWN_TICKS=36000）；冷却结束后每 100 tick 在随机在线玩家 48~96 格外补刷一只（SpawnPlacements.isSpawnPositionOk 验位，候选：僵尸/骷髅/蜘蛛/苦力怕/女巫/末影人，年限百万~9999万随机）
- **年限生效**：scale 属性加成（1+0.15×log10(age+1)，十年≈1.16→9999万年≈2.20，每档 +0.15 明显递增）+ 攻击力加成（max(0, 2×log10(age+1)-2)，百年+2→9999万年+14）+ 百万年 setPersistenceRequired 防消失；修饰符幂等应用（先 removeModifier 再加）
- **生成拦截点**：EntityJoinLevelEvent（NATURAL/CHUNK_GENERATION/EVENT 才魂兽化；铁傀儡特例：村庄生成与玩家建造都纳入、刷怪蛋除外；刷怪笼/结构/命令生成的其他生物跳过；hasData 已魂兽化的读档时重新幂等应用 modifier 后跳过，旧存档生物同步新曲线）
- **客户端同步**：PlayerEvent.StartTracking 时发 SoulBeastSyncPayload(entityId, uuid, age)；客户端缓存 UUID 校验防实体 ID 复用错配，每 40 tick 清理失效条目
- **渲染**（SoulBeastRingRenderer，AFTER_ENTITIES）：每只魂兽脚底一个旋转魂环（entityTranslucentEmissive，按年限档位选 SoulRingAge 6 档贴图；半径 = bbWidth×0.5+0.45+0.10×log10(age+1)，getBbWidth 已含 scale）；头顶 48 格内显示年限文字（SEE_THROUGH + 半透明黑底 + 档位色：十年白/百年黄/千年紫/万年灰/十万年红/百万年金；颜色必须带 alpha 分量 0xFF，否则 Font 渲染完全透明；文字朝向必须乘 entityRenderDispatcher.cameraOrientation()，误乘相机 rotation 差 180° 背对玩家不可见；格式 X年/X.X万年）；64 格内渲染魂环；6 档 RenderType 静态缓存
- **掉落**：LivingDropsEvent 中魂兽死亡原地掉落魂环物品（CUSTOM_DATA 写 SoulBeastName=显示名、SoulRingAge=年限；显式 setPickUpDelay(0) 立即可拾取，后续版本改为右键吸收），tooltip 显示《魂兽：xx》《魂环年限：xx》（黄/金色）
- **物品贴图**：soul_ring 物品模型带 5 个 override（ring_tier 1~5 → 6 张贴图 textures/item/soul_ring_decade…megennium.png，64×64 圆环，由 PowerShell System.Drawing 脚本生成，脚本已删除）；SoulRingRenderer.drawFlatQuad 改为 public static 供魂兽渲染器复用
- 语言文件双语同步：item.soul_ring / soul_ring.beast / soul_ring.age
- 构建验证通过（build SUCCESSFUL）

**注意事项**：
- **NeoForge 21.1.29 无 MobSpawnEvent.FinalizeSpawn**（只有 SpawnPlacementCheck/PositionCheck），拦截生成用 EntityJoinLevelEvent + Mob.getSpawnType() 判断；MobSpawnEvent 基类也无 getPos()（只有 getX/Y/Z double）
- **spawnReason 不持久化**：存档重载的原版生物 spawnType 默认 NATURAL，会被魂兽化一次（模组安装前已存在的生物）；魂兽读档靠 hasData(SOUL_BEAST) 跳过
- **1.21.1 SavedData**：save(CompoundTag, HolderLookup.Provider) 是抽象方法必须实现；Factory 构造为 (Supplier, BiFunction<CompoundTag, HolderLookup.Provider, T>) 而非旧版 (Supplier, Codec)，本类手写 NBT 读写绕过 NbtOps
- **AttachmentType.builder(Class::new) 方法引用歧义**（Supplier 与 Function<IAttachmentHolder,T> 均匹配），必须写显式 lambda `() -> new Xxx()`
- **FriendlyByteBuf 才有 writeVarInt/readVarInt**：自定义 payload 的 StreamCodec 泛型用 FriendlyByteBuf 而非 ByteBuf
- **NaturalSpawner.isSpawnPositionOk 已移除**：1.21.1 改用 SpawnPlacements.isSpawnPositionOk(EntityType, LevelReader, BlockPos)（net.minecraft.world.entity 包）
- 概率权重/冷却时长/补刷距离等参数集中在 SoulBeastAge 与 SoulBeastWorldData/SoulBeastSpawnHandler 常量，调平衡只改这些
- 魂兽环与玩家环共用 6 档贴图与 drawFlatQuad；魂兽环旋转每只按实体 ID 相位错开，避免齐排
- **AFTER_ENTITIES 阶段 pose 已含视图旋转**：billboard 文字必须乘 entityRenderDispatcher.cameraOrientation()（与原版 EntityRenderer.renderNameTag 一致）；误乘 gameRenderer.getMainCamera().rotation()（相机旋转）在 yRot=0 时相差 180°，文字背对玩家不可见
- PowerShell 脚本含中文注释且无 BOM 会被 PS 5.1 按 ANSI 解析导致部分行损坏：临时脚本必须纯 ASCII，且执行时用 -File + 单引号路径（含空格路径双引号会被拆分）

---

## 18. 魂环掉落改实体 + 头顶命名牌重做 + 玩家死亡配置（2026-08-15）

**明细**：
- **魂环掉落改为魂环实体**（用户澄清：要的是环状实体不是掉落物，不可拾取是预期行为，后续版本右键吸收）：新建 `entity/SoulRingEntity`（Entity 子类：synchedData 年限同步 + NBT 持久化 + customName 命名牌）、`client/SoulRingEntityRenderer`（地面旋转发光环 + 直接复用原版 protected renderNameTag 画环上方年限文字）、`client/ClientEntityRenderers`（RegisterRenderers 注册）；ModEntities 注册 SOUL_RING_ENTITY（MobCategory.MISC）；onLivingDrops 改为原地生成魂环实体，删掉 SoulRingItem 掉落逻辑（物品类保留备用）
- **头顶文字三轮修复失败后重做**：根因是 AFTER_ENTITIES 阶段手动画的文字批次不 endBatch 会被帧末 crushRebuffers 丢弃；彻底改用**原版命名牌机制**——魂兽服务端 setCustomName + setCustomNameVisible(true)，由原版生物渲染器自动画头顶（与命名牌同管线，必然显示）；SoulBeastRingRenderer 删除全部手动画文字代码（renderNameTag/NAME_* 常量），只留脚底魂环
- **头顶内容改为「名称+年限」**（如「史莱姆 4281年」「铁傀儡 4.6万年」）：名称用 EntityType.getDescription()（translatable 组件，客户端按客户端语言本地化）；不能取 getName（会被 customName 覆盖，幂等重设时叠加成「僵尸 987年 987年」）；Entity.getTypeName() 是 protected 不可用
- **玩家死亡魂师身份 config**：新建 `config/ModConfig`（ModConfigSpec）+ 主类构造器注入 ModContainer 调 registerConfig（21.1 新 API，旧 ModLoadingContext.registerConfig 已移除）；配置文件 config/all_spirit_continent-server.toml 的 clearSoulMasterOnDeath（默认 false=死亡保留魂师身份与魂环；true=重置 PlayerLevelData 全部数据连魂环一起删）；onPlayerClone 死亡分支按配置手动 copyFrom/reset（不依赖 copyOnDeath，保证开关生效），非死亡克隆完整复制
- PlayerLevelData 新增 copyFrom（整体复制含魂环）/reset（重置为全新状态）方法

**注意事项**：
- **NeoForge 21.1.29 FML 类不在 compiledWithNeoForge jar**：ModContainer/IConfigSpec 等在独立依赖 net.neoforged.fancymodloader:loader-4.0.24.jar，javap 需用该 jar；ModLoadingContext 已无 registerConfig 方法
- **registerConfig 三参版本**：ModContainer.registerConfig(ModConfig.Type, IConfigSpec, String) 第三参是配置文件名（含 .toml）
- **原版 EntityRenderer.renderNameTag 是 protected**：自定义实体渲染器可直接调用（实体渲染上下文批次可靠）；1.21.1 高度锚点来自 EntityAttachment.NAME_TAG（默认实体顶部）+0.5，缩放 0.025，cameraOrientation 朝向
- customName 的 TextColor 不生效（原版 renderNameTag 颜色固定白色），头顶文字颜色需后续自定义渲染才可实现

---

## 19. 击杀校验 + Boss 百万年 + 自定义魂环配置 + 生命值加成（2026-08-15）

**明细**：
- **击杀校验（玩家参与击杀才掉魂环）**：LivingHurtEvent 记录玩家伤害魂兽的游戏 tick（source.getEntity() instanceof Player，玩家箭矢等间接伤害 getEntity 即发射者；LAST_PLAYER_DAMAGE ConcurrentHashMap 内存缓存不持久化）；LivingDropsEvent 检查死亡前 30 秒（PLAYER_KILL_WINDOW_TICKS=600）内有玩家伤害才生成魂环实体，否则铁傀儡/环境击杀不掉；remove 同步清理条目
- **Boss 固定百万年**：BOSS_TYPES = 末影龙/凋零/监守者（Set<EntityType<?>>），onEntityJoin 优先级②——无视生成方式与新手保护，固定 BOSS_AGE=1_000_000 直接 applySoulBeast（不参与百万年配额降级校验，只正常计数）；**其他 mod 兼容**：魂兽化判断本就对任意 Mob 生效，mod 生物的年限表/名称/配置均通用，mod boss 通过自定义配置指定年限
- **自定义魂环配置文件（优先级最高）**：新建 `config/SoulRingConfig`，注册为第二个 SERVER 配置（config/all_spirit_continent-soul_ring.toml）；customSoulRingOverrides 列表每条格式「生物ID=最小年限-最大年限」（闭区间随机）或「生物ID=年限」（固定），ID 为 EntityType 注册名（支持其他 mod 生物）；onEntityJoin 优先级①——配置匹配即魂兽化（无视生成方式过滤/新手保护/概率表）；解析懒缓存（CACHE volatile + 双检锁，配置重启才生效），格式非法 LOGGER.warn 跳过；comment 中带模板示例（# minecraft:zombie=100-1000 等四行）
- **生命值随年限提升**：SoulBeastAge.healthBonusOf = max(0, 5×log10(age+1)-5)（百年+0/千年+5/万年+10/十万年+15/百万年+20/千万年+25/9999万年+30）；applyModifiers 加 MAX_HEALTH 幂等修饰（HEALTH_MOD_ID）+ 当前血量 clamp 到新上限
- onEntityJoin 流程重排：hasData 跳过 → ①自定义配置 → ②Boss → ③默认概率表；ServerLevel 转换提前（前两者也需要 level）

**注意事项**：
- **自定义配置优先级语义**：配置匹配的怪直接魂兽化，刷怪笼/刷怪蛋等生成方式也不拦截（整合包作者主动配置即默认接受），但会绕过百万年配额校验与新手保护
- Boss 走 applySoulBeast 会触发 isMillion → onMillionSpawned 计数 + setPersistenceRequired；末影龙复活/凋零召唤正常占用百万年配额
- LivingHurtEvent 先于 LivingDeathEvent/LivingDropsEvent 触发，30 秒窗口内的玩家伤害都计入「参与击杀」（含玩家打了一部分后铁傀儡补刀）

---

## 20. 魂兽头顶血量第二行 + 调试棒属性查看面板（2026-08-15）

**明细**：
- **血量第二行（Mixin 注入原版命名牌管线）**：新建 `mixin/client/EntityRendererMixin`，@Inject 原版 `EntityRenderer.renderNameTag` 末尾（TAIL）——魂兽头顶命名牌（名称+年限）下方 9 像素画第二行「当前血量 / 最大血量」；与命名牌同锚点（`EntityAttachment.NAME_TAG` +0.5）、同朝向（相机 rotation）、同缩放（0.025）；颜色按血量比例：>50% 绿（0xFF55FF55）/ >25% 黄（0xFFFFFF55）/ 否则红（0xFFFF5555）；仅对客户端缓存中有年限记录的魂兽生效（`SoulBeastClientCache.getAge > 0`），不影响玩家与普通命名牌生物
- **Mixin 基础设施（NeoForge 21.1 / moddev）**：`neoforge.mods.toml` 启用 `[[mixins]] config="${mod_id}.mixins.json"`；新建 `resources/all_spirit_continent.mixins.json`（required / minVersion=0.8.5 / package / compatibilityLevel=JAVA_21 / client 列表）；**不需要**在 build.gradle 手动加 mixin 依赖或 annotationProcessor——moddev 自动提供编译依赖；手动加 AP 会报「Unable to locate obfuscation mapping for @Inject target」编译错误（moddev 不传 reobfSrgFile）
- **调试棒属性查看面板**：新建 `client/SoulBeastInspectorOverlay`（纯客户端 HUD，`RegisterGuiLayersEvent.registerAboveAll`）：手持调试棒（主手或副手 `TiaoShiBangItem`）且准星指向魂兽（复用原版每帧已算好的 `mc.hitResult` EntityHitResult + 缓存年限>0）时，在准星下方 20 像素水平居中显示半透明黑底面板 5 行：名称(金) / 魂环年限(白) / 攻击力(红) / 血量(绿) / 体型(蓝)；数据全部客户端可得（属性 synced=true、血量实时同步、年限缓存），**零额外网络包、零额外射线开销**；后续新增特殊属性在 render 里追加行即可
- 语言文件新增 4 条 inspector.* 条目（中英同步）
- 编译验证通过 + runClient 启动无 Mixin 注入报错（游戏内视觉效果待实测）

**注意事项**：
- **dev 环境（runClient）类名未混淆**：@Inject 按 named 名直接匹配生效，mixins.json 不需要 refmap 字段（写了反而产生运行时警告）；**生产构建发布时需补 refmap**（待办：研究 moddev 官方 mixin AP 配置方式，为混淆环境生成 refmap）
- 血量第二行复用 renderNameTag 传入的 MultiBufferSource（字体批次与命名牌同管线），绘制时机天然正确，无需手动 endBatch；朝向与原版一致用 `gameRenderer.getMainCamera().rotation()`
- 调试棒查看面板的准星命中直接读 `mc.hitResult`，不要自己发射线（每帧重复射线浪费性能）

---

## 21. 魂兽血量/攻击力改乘算大倍数 + 魂兽化回满血（2026-08-15）

**明细**：
- 用户反馈：血量和攻击力太低；要求 1 年 = 原版生物原本值，9999 万年 = 血量几十万、攻击十几万，按年限匹配
- `SoulBeastAge`：删除 `attackBonusOf`/`healthBonusOf`（加算），新增乘算倍数函数：
  - `attackMultiplierOf(age) = max(age,1)^0.6`：十年约 ×4、百年 ×16、千年 ×63、万年 ×251、十万年 ×1000、百万年 ×3981、千万年 ×1.6万、9999万年 ×6.3万（僵尸基础 3 攻 → 9999 万年约 19 万攻击）
  - `healthMultiplierOf(age) = sqrt(max(age,1))`：十年约 ×3.2、百年 ×10、千年 ×32、万年 ×100、十万年 ×316、百万年 ×1000、千万年 ×3162、9999万年 ×10000（僵尸基础 20 血 → 9999 万年约 20 万血）
- `SoulBeastSpawnHandler.applyModifiers`：攻击力/生命值修饰符由 ADD_VALUE 改为 **ADD_MULTIPLIED_TOTAL**（值 = 倍数 - 1）；应用后 `setHealth(getMaxHealth())` **回满血**（不再出现「上限 29 当前 20」）
- 编译验证通过

**注意事项**：
- **NeoForge 21.1.29 的 `AttributeModifier.Operation` 没有 `MULTIPLY_TOTAL`**：原版枚举名被 NeoForge 重命名为 `ADD_MULTIPLIED_TOTAL`（语义相同：final = base ×(1+Σ ADD_MULTIPLIED_TOTAL)）；枚举真实值用 javap 查 `build/moddev/artifacts/neoforge-21.1.29-merged.jar` 确认（`.gradle` 缓存里 neoformruntime 的 jar 全是混淆名，javap 必须用 moddev artifacts 下的 merged jar）
- 乘算倍数基于生物原本基础值：不同生物自动按自身基础缩放（僵尸 20 / 铁傀儡 100 / 监守者 500），无需逐怪配置
- 读档重载的老魂兽也会重新幂等应用并回满血：打残的魂兽退出重进会满血（按用户「血量默认恢复满」要求）
- 数值平衡注意：百年魂兽即达到原版十几倍攻血，玩家原版装备无法硬钢；后续玩家侧武魂/魂环成长体系需要跟上此数值模型

---

## 22. 血攻共用同一倍数（保持原版比例）+ 9999万年血量约 5000 万（2026-08-15）

**明细**：
- 用户反馈：血量还是太少；要求 9999 万年血量约 **5000 万**；每一档年限之间增幅要明显；攻击力也高但不能失衡，按**原版血量与攻击力的比例**增加
- `SoulBeastAge`：删除 `attackMultiplierOf`（^0.6）与 `healthMultiplierOf`（开方）两个不同指数的函数，合并为 **`statMultiplierOf(age) = max(age,1)^0.8`**（血量与攻击力共用同一倍数，血攻比例天然保持生物自身的原版比例，如僵尸 20:3）
  - 十年约 ×6.3、百年约 ×40、千年约 ×251、万年约 ×1585、十万年 ×10000、百万年约 ×6.3万、千万年约 ×40万、9999万年约 ×250万
  - 每升一个数量级 ×10^0.8≈×6.3，每档增幅明显
  - 僵尸（20 血 / 3 攻）到 9999 万年：约 **5000 万血 / 750 万攻**（血攻比 6.67:1 与原版一致）
- `SoulBeastSpawnHandler.applyModifiers`：两处调用改 `statMultiplierOf`（ADD_MULTIPLIED_TOTAL 不变）
- 编译验证通过

**注意事项**：
- 之前两函数指数不同（攻 ^0.6、血开方）导致 9999 万年时血 20 万/攻 19 万，比例几乎 1:1 与原版脱节；统一指数后血攻比自动保持
- 调数值只改 `statMultiplierOf` 的指数 0.8：想更高就加大（如 0.85），想平滑就减小；血攻必须同倍数才能保持原版比例
- 5000 万血量会超过 int 范围吗：5000 万 < 21 亿，int 显示无问题，但 HUD 面板「当前血量/最大血量」两位整型显示 8 位数（`(int)` 转换安全）

---

## 23. 解除原版属性上限 + 攻击力同步到客户端（2026-08-15）

**明细**：
- 用户反馈：血量最多只有 1024，攻击力依旧个位数
- **根因排查**（服务端诊断日志 `[SoulBeast-apply]` + javap 查 `Attributes` 类字节码）：
  - 原版 `generic.max_health` 是 `RangedAttribute(20, 1, 1024)`、`generic.attack_damage` 是 `RangedAttribute(2, 0, 2048)`，`sanitizeValue` 把乘算后的属性值裁剪到上限——日志中 maxHealth 恒 1024、attack 恒 2048
  - 原版 `Attributes.bootstrap` 只对 `max_health` 调用 `setSyncable(true)`，`attack_damage` 未开启同步 → `AttributeMap` 同步集合（getAttributesToSync/getAttributesToUpdate）不含攻击力 → 客户端 `getAttributeValue(ATTACK_DAMAGE)` 永远返回基础值（僵尸 3）
- 新建两个 common Mixin（`mixins.json` 新增 "mixins" 公共段）：
  - `mixin/RangedAttributeMixin`：@Inject `sanitizeValue` HEAD cancellable，按 `getDescriptionId()` 识别 `attribute.name.generic.max_health` / `attribute.name.generic.attack_damage`，对这两个属性跳过裁剪（仅保底非负），其余属性维持原版
  - `mixin/AttributesMixin`：@Inject `Attributes.bootstrap` TAIL，`ATTACK_DAMAGE.value().setSyncable(true)`（所有实体创建前生效）
- 验证：enderman age=85442 → attack=61721.3 / maxHealth=352693.2；百万年末影龙 maxHealth=12619147.0；血攻按年限正确乘算

**注意事项**：
- `Attribute` 构造器是 protected（String, double）且无无参构造：Mixin 类 `extends Attribute` 必须显式声明同签名构造器，否则编译报「无法将类 Attribute 中的构造器应用到给定类型」
- **注入有返回值的方法必须用 CallbackInfoReturnable**：`bootstrap` 返回 `Holder<Attribute>`，用 CallbackInfo 会在 Mixin APPLY 阶段崩溃（Invalid descriptor: CallbackInfoReturnable is required）
- `sanitizeValue` 在服务端与客户端计算属性值时都会调用，解除上限的 Mixin 放公共段保证两端一致
- 攻击力 syncable 后所有实体（含玩家）的 ATTACK_DAMAGE 都会多同步一个属性，无副作用
- 末影龙无 ATTACK_DAMAGE 属性（日志 attack=N/A 正常），其伤害走 DragonHoldingPattern 等特殊机制

---

## 24. 「魂视」附魔：头盔专属 + 穿戴查看魂兽信息（2026-08-15）

**明细**：
- 用户需求：新增一个附魔书（名称自定），只能附在头盔上，玩家穿戴后可以看到魂兽详细信息
- 新建 `enchantment/ModEnchantments`：`SOUL_VISION`（注册名 soul_vision，中文名「魂视」）；1.21.1 Enchantment 是 record，四参构造（description / definition / exclusiveSet / effects），搭配 `Enchantment.definition(...)` 静态工厂；**仅头盔**：supportedItems=`BuiltInRegistries.ITEM.getOrCreateTag(ItemTags.HEAD_ARMOR)` + slots=`EquipmentSlotGroup.HEAD`；maxLevel 1、weight 10（稀有）、铁砧费 4
- `SoulBeastInspectorOverlay` 触发条件放宽：手持调试棒（原）**或**穿戴「魂视」头盔（新）——`EnchantmentHelper.getItemEnchantmentLevel(SOUL_VISION, mc.player.getItemBySlot(EquipmentSlot.HEAD)) > 0`，面板内容不变（名称/年限/攻击力/血量/体型）
- 主类注册 `ModEnchantments.ENCHANTMENTS`；语言文件中英同步 `enchantment.all_spirit_continent.soul_vision`（魂视/Soul Vision）
- 获取：`/give @p minecraft:enchanted_book[minecraft:stored_enchantments={levels:{"all_spirit_continent:soul_vision":1}}]` 后铁砧附到任意头盔
- 编译 + runClient 启动验证通过（附魔注册无报错）

**注意事项**：
- **1.21.1 没有 `BuiltInRegistries.acquireBootstrapRegistrationLookup`**（那是 1.21.2+ API）：mod 侧 DeferredRegister 注册发生在 RegisterEvent（注册表冻结前），用 `BuiltInRegistries.ITEM.getOrCreateTag(ItemTags.HEAD_ARMOR)` 拿 HolderSet——tag 引用先创建、内容在数据包加载时绑定，与 bootstrap 语义相同
- 1.21.1 Enchantment 直接四参构造即可，不需要 `Enchantment.enchantment(definition).build(resourceKey)` Builder 链（Builder 的 build 需要 ResourceKey，DeferredRegister 自动分配）
- 附魔台附魔默认需要 supportedItems + slots 双限制；铁砧附魔书只校验 supportedItems 与槽位兼容

---

## 25. 修正「魂视」附魔注册方式：DeferredRegister 改为数据包 JSON（2026-08-15）

**明细**：
- 用户反馈：创造物品栏没找到附魔书；日志中 `/give` 报「Failed to get element all_spirit_continent:soul_vision」
- **根因**：1.21.1 附魔是**数据驱动的动态注册表**（`RegistryDataLoader.WORLDGEN_REGISTRIES` 含 ENCHANTMENT，由数据包 JSON 加载）；而 NeoForge 21.1.29 的 `RegisterEvent` 只在 `GameData.postRegisterEvents()` 中对 `BuiltInRegistries`（静态注册表）发出 → `DeferredRegister.create(Registries.ENCHANTMENT)` 的 addEntries 永远不会被调用，附魔从未注册且**完全静默**（无任何报错）
- **修复**：
  - 新建 `data/all_spirit_continent/enchantment/soul_vision.json`（参照原版格式：description/supported_items=`#minecraft:enchantable/head_armor`/weight 10/max_level 1/min_cost 15/max_cost 30/anvil_cost 4/slots=["head"]）
  - `ModEnchantments` 重写：删除 DeferredRegister，改为 `SOUL_VISION_KEY`（ResourceKey）
  - 主类删除 `ENCHANTMENTS.register(modEventBus)`
  - `SoulBeastInspectorOverlay`：`mc.level.registryAccess().lookupOrThrow(Registries.ENCHANTMENT).getOrThrow(SOUL_VISION_KEY)` 取 Holder 后检测
  - `ModCreativeTabs`：模组标签页直接加入魂视附魔书（`EnchantedBookItem.createForEnchantment(new EnchantmentInstance(params.holders().lookupOrThrow(Registries.ENCHANTMENT).getOrThrow(KEY), 1))`），一眼可见；同时原版工具标签页也会自动生成附魔书变体

**注意事项**：
- **NeoForge 21.1.29 对动态注册表（附魔等）的 DeferredRegister 注册静默无效**：`new RegisterEvent` 全源码仅 GameData 一处、只遍历静态注册表；排查方法：javap/sources 搜 `new RegisterEvent` 确认事件发出范围
- give 命令报「Failed to get element X」即注册表 lookup 中无该附魔（`Enchantment.CODEC` 是 RegistryFixedCodec，解析时查动态注册表）
- 代码中不能用 DeferredHolder 引用附魔：必须按 ResourceKey 从 registryAccess 的 lookup 中获取（客户端 registryAccess 由服务端同步，含 mod 数据包内容）
- 附魔 JSON 的 supported_items 用 `#minecraft:enchantable/head_armor`（原版附魔同款标签），不是 `#minecraft:head_armor`

---

## 26. 成就页「全魂大陆」：觉醒武魂 + 每十级境界成就链（2026-08-15）

**明细**：
- 用户需求：新增成就页，第一个成就《觉醒武魂？！》（条件：获得水晶球），分支 1 每十级一个成就《魂士》（条件：获得第一个魂环）以此类推
- **成就树结构**（`data/all_spirit_continent/advancement/`，全部数据包 JSON 驱动）：
  - `root.json`：成就页入口「全魂大陆」（图标魂环物品，背景 `textures/gui/advancements/backgrounds/soul_ring.png`，criteria 用 `minecraft:tick` 自动完成，不显示 toast）
  - `awaken.json`：《觉醒武魂？！》——`minecraft:inventory_changed` + items=`all_spirit_continent:shuijingqiu`，获得水晶球自动完成，纯数据无需代码
  - `soul_rank/rank_1~11.json`：《魂士》→《魂师》→《大魂师》→《魂尊》→《魂宗》→《魂王》→《魂帝》→《魂圣》→《魂斗罗》→《封号斗罗》→《极限斗罗》，criteria 用 `minecraft:impossible`（不自动触发），由代码授予；图标用原版物品阶梯（铁剑→金剑→钻石剑→下界合金剑→金苹果→附魔金苹果→信标→龙头→下界之星→不死图腾），封号斗罗 frame=goal、极限斗罗 frame=challenge
- 新建 `util/ModAdvancements`：`awardSoulRank(ServerPlayer, int level)` 按等级阈值表 {10,1}~{99,11} 授予全部已达标境界成就（`player.getServer().getAdvancements().get(id)` 取 AdvancementHolder → `player.getAdvancements().award(holder, "requirement")`，重复授予无害）
- `TiaoShiBangItem.grantSoulRing` 等级 +10 后调用 `ModAdvancements.awardSoulRank((ServerPlayer) player, data.getLevel())`
- 背景贴图 `soul_ring.png`：PowerShell + System.Drawing 生成 16×16（深紫黑底 + 金色双圆环）
- 语言文件中英同步 26 个成就文本键（advancement.all_spirit_continent.*）；编译通过

**注意事项**：
- 1.21.1 成就完全由数据包 JSON 定义，无需代码注册（与附魔同为动态注册表，但 DeferredRegister 对附魔无效的原因不适用于成就——成就本来就没有代码注册 API）
- 根成就 criteria 可用 `minecraft:tick`（自动完成）；不写 background 时无自定义成就页
- 成就 icon 格式：`{"count": 1, "id": "..."}`（1.21.1 还是 id/count，1.21.2+ 才改 components）
- 代码授予成就用 `PlayerAdvancements.award(AdvancementHolder, criterionKey)`，criterionKey 必须与 JSON criteria 的键一致（这里统一为 "requirement"）
- 等级 90→99 一次跳跃会同时授予封号斗罗（91）与极限斗罗（99）两个成就，属正常

---

## 27. 成就链重构：魂士=使用水晶球，封号/超级/极限斗罗按新等级段（2026-08-15）

**明细**：
- 用户调整：使用完水晶球 →《魂士》；《魂师》=第一个魂环（10级）……《魂斗罗》=第八个魂环（80级）；《封号斗罗》=获得第九个魂环（90级）；《超级斗罗》=95级；《极限斗罗》=99级
- `ModAdvancements`：等级阈值表改为 {10,2}~{80,9}、{90,10}封号、{95,11}超级、{99,12}极限；新增 `awardSoulMaster(ServerPlayer)` 授予《魂士》（rank_1，不在等级表中）
- `ShuiJingQiuItem.finishUsingItem`：服务端分支调用 `awardSoulMaster`（使用完水晶球即授予，重复使用无害）
- `TiaoShiBangItem.grantSoulRing`：**90 级（9 环满）后改为每次 +1 级修为**（91~99 逐级，无新魂环），否则 90→99 跳级导致 95-98 超级斗罗段无法体验；提示文案区分「你获得了一个魂环！」/「修为提升！当前等级：%s」（新增语言键 soul_power_up）
- 成就 JSON：rank_1 图标改经验瓶（魂士未获环）、rank_2 改魂环物品（魂师=第一环）、rank_3~10 图标整体移位；rank_11 重写为超级斗罗（nether_star，frame=goal）；新建 rank_12 极限斗罗（totem_of_undying，frame=challenge）；语言文件中英同步（rank_1~12 共 24 键 + soul_power_up）
- 编译通过

**注意事项**：
- 90 级前调试棒每 +10 一级环：10,20,…,80,90；90 级后魂环满 9 个（ringCount 保持 9），+1 修为到 99——称号系统 91-94 封号斗罗、95-98 超级斗罗、99 极限斗罗与本成就链对齐
- 《觉醒武魂？！》（获得水晶球）与《魂士》（使用完水晶球）是两个独立成就，先后连续达成
- 成就 JSON 的 title/description 只写 translate 键，改文案只需改语言文件；图标改动则改各 rank_N.json 的 icon.id

---

## 28. 成就图标改用魂环年限贴图：十年→百万年逐级递增（2026-08-15）

**明细**：
- 用户需求：成就图标全部使用 `textures/item` 文件夹里的魂环年限贴图——魂师到封号斗罗每一级用一个，最后几个全用百万年
- **实现方式（无需注册新物品）**：成就 icon 是 ItemStack codec，直接给现有 `soul_ring` 物品附加 `minecraft:custom_data`
 组件写 `SoulRingAge` 年限值，渲染时全局 `ring_tier` predicate 读到年限 → override 自动切到对应年限贴图
  - `rank_1` 魂士：icon 改 `all_spirit_continent:shuijingqiu`（水晶球，使用完水晶球成就）
  - `rank_2` 魂师：`SoulRingAge: 10`（十年白）
  - `rank_3` 大魂师：`100`（百年黄）
  - `rank_4` 魂尊：`1000`（千年紫）
  - `rank_5` 魂宗：`10000`（万年灰）
  - `rank_6` 魂王：`100000`（十万年红）
  - `rank_7~12` 魂帝→魂圣→魂斗罗→封号斗罗→超级斗罗→极限斗罗：`1000000`（百万年金）
- 验证：原版 1.21.1 成就 `craft_decorated_pot_using_only_sherds.json` 的 icon 带 components 先例确认格式；全部 14 个成就
  JSON PowerShell ConvertFrom-Json 语法校验通过；runClient 启动无成就加载错误

**注意事项**：
- 1.21.1 成就 icon 已支持 components 字段（ItemStack codec，原版 decorated_pot 成就先例），无需像旧版那样只能用物品 id
- 成就界面渲染 icon 走 ItemRenderer，`SoulRingItemProperties` 全局注册的 `ring_tier` predicate 对所有该物品的 ItemStack
 生效（含成就界面），故 components 指定年限即可换色，比注册 6 个独立年限物品轻量得多
- icon components 的 custom_data 数值为 NBT IntTag（JSON 数字），与 SoulRingItem.getRingAge 读取路径一致

---

## 29. 玩家成长体系：血量/精神力随等级提升 + 魂环年限血量加成（2026-08-15）

**明细**：
- 用户需求：玩家血量和精神力根据等级提升而增加；获得魂环可获得对应年限的血量加成，年限越高加成越高，9 个魂环加成独立
- 新建 `util/SoulGrowth`（成长公式集中工具类）：
  - 血量 = 20 基础 + 等级加成（每级 +2，99 级共 +196）+ 魂环年限加成
  - 魂环年限加成（每环独立累加，未获得的环不算）：十年 +2 / 百年 +4 / 千年 +8 / 万年 +16 / 十万年 +32 / 百万年 +64（2 的幂）
  - 精神力上限 = 100 + (等级 - 1) × 10（99 级 = 1080）
  - `applyHealth(Player)`：幂等应用血量加成——先 `removeModifier(HEALTH_MOD_ID)` 再 `addTransientModifier(ADD_VALUE)`
- `PlayerLevelData.setLevel`：等级变化时重算精神力上限并回满（`SoulGrowth.maxSpiritPower(level)`）；全参构造器（Codec 反序列化）同步重算，旧存档固定值被新公式覆盖，Codec 格式不变
- `TiaoShiBangItem`：grantSoulRing（升级/获得魂环）与 increaseRingAge（年限提升）调用 `applyHealth` 后 `setHealth(max)` 回满血；removeSoulRing 只重算不回满
- `All_spirit_continent`：onPlayerLogin / onPlayerClone 调用 `applyHealth`（transient 修饰符不随 NBT 持久化，重进世界/换维度/死亡重生必须重新应用；死亡清魂师身份时 bonus=0 自动移除）
- `LevelHudOverlay`：血量显示从百分比改为「当前/上限」具体数值（如 20/216），直观看到成长
- 编译通过；runClient 启动无错误

**注意事项**：
- 玩家 max_health 用 **transient** 修饰符（非 permanent）：不写玩家 NBT，由登录/克隆事件按存档数据重算，避免 NBT 与 Attachment 双份存储不一致；同 ResourceLocation ID 先移除再加，天然幂等
- 原版 max_health 增加不会自动回血（仅超限时裁剪），升级回满血需手动 `player.setHealth(player.getMaxHealth())`
- 玩家基础血量 20 与魂兽（百万血）仍有差距，属预期——玩家侧武魂/魂环属性成长体系是后期规划，数值在 SoulGrowth 常量中集中可调
- 满配参考：99 级 + 9×百万年环 = 20 + 196 + 576 = 792 血；1 级 = 20 血 / 100 精神力

---

## 30. 魂环加成改为魂兽量级：年限连续公式 + 非整数级魂环支持（2026-08-16）

**明细**：
- 用户反馈：第 29 条的魂环加成太少，应按「对应年限魂兽的血量/精神力」量级给玩家加（如百万年魂兽几十万血 → 魂环加几十万血）；且魂环年限应从档位改为具体年限（当前默认整数级，后续支持非整数级如 6070 年）
- **核心改造：环年限从 6 档位（0~5）改为具体年限 int（0=无环，十年=10…百万年=1000000）**
  - `PlayerLevelData`：ringAges 存年限；setRingAge clamp 到 [0, MAX_AGE]（不再取模）；全参构造器自动迁移旧档位（1→100、2→1000、3→10000、4→100000、5→1000000）；新增 `refreshSpiritPower()`（环年限变化后重算精神力并回满）
  - `SoulRingAge.getTexture(age)`：参数语义改为年限，内部 tierOf 转档位；新增 `getTierTexture(tier)` 供魂兽渲染按档位取贴图（修复 SoulBeastRingRenderer 旧语义调用）
  - `SoulRingEntityRenderer`：getTexture 传年限（不再预转档位）
- **加成公式改为魂兽血攻同曲线**（SoulGrowth）：
  - 单环血量加成 = 基准 20 × 年限^0.8（十年≈126 / 百年≈800 / 千年≈5030 / 万年≈3.2万 / 十万年=20万 / 百万年=126万）
  - 单环精神力加成 = 基准 1 × 年限^0.8（百万年≈6.3万；初版基准 100 实测太高，用户反馈后缩减 100 倍）
  - 精神力上限 = 100 + (等级-1)×10 + 已获魂环年限加成
  - 年限直接代入公式，非整数级魂环天然支持（未来魂环吸收玩法直接传入真实年限）
- `TiaoShiBangItem`：年限修改改为 ×10 阶梯循环（10→100→1000→10000→100000→1000000→回10），提示用 SoulBeastAge.format（支持「1.2万年」等非整数显示）；grantSoulRing 新环默认十年并 refreshSpiritPower
- `LevelHudOverlay`：血量/精神力大数值格式化为「X.X万」（如 126.2万/126.2万）
- 编译通过

**注意事项**：
- **旧存档迁移**：旧档位 1~5 在 Codec 反序列化时映射为具体年限；档位 0（旧十年环）保持 0，渲染 tierOf(0)=0 十年贴图，但加成公式 age<=0 返回 0（十年环旧档位 0 会丢失 126 点加成，新环从 10 起无此问题，影响可忽略）
- 满配参考：99 级 + 9×百万年环 = 20 + 196 + 9×126万 ≈ 1134万血、精神力 ≈ 56.8万——血量与高年限魂兽量级对齐，精神力按缩减 100 倍后的基准 1 计算
- 魂环渲染：玩家环（SoulRingRenderer）与魂兽环（SoulBeastRingRenderer）贴图语义已拆分（年限 vs 档位），修改时注意区分

---

## 31. 魂环攻击力加成 + 魂帝精神力飞行 + 重生回满血（2026-08-16）

**明细**：
- **魂环攻击力加成**（SoulGrowth）：单环攻击力 = 基准 2 × 年限^0.8（血量基准的 1/10，与魂兽血攻比例同量级；十年≈12.6 / 百年≈80 / 千年≈503 / 万年≈3155 / 十万年≈2万 / 百万年≈12.6万），9 环独立累加；`applyAttack()` 用 transient 修饰符加到玩家 ATTACK_DAMAGE（ID `soul_growth_attack`，幂等）；属性上限 2048 已由 RangedAttributeMixin 全局解除、攻击力客户端同步已由 AttributesMixin 开启，玩家无需额外处理
  - 调用点：主类登录/克隆事件、调试棒获取/移除魂环/年限修改三个模式
- **魂帝精神力飞行**（新建 `util/SoulFlight.java` + 主类 PlayerTickEvent.Post）：61 级（魂帝，TitleSystem.SOUL_EMPEROR_LEVEL）起解锁飞行能力（mayfly），创造/旁观不干预；真正飞行中每秒消耗 2 点精神力（每 20 tick 结算）；精神力每 2 秒回复 1 点（已激活玩家，不超上限，变化时同步 HUD）；精神力不足 2 点时强制落地并提示；能力状态变化时才 onUpdateAbilities 同步，避免每 tick 发包
  - 语言键：flight_unlock（解锁提示，首次获得飞行能力时提示）/ flight_no_spirit（精神力不足落地提示）
- **重生回满血**（主类 onPlayerClone）：死亡重生（isWasDeath）时 applyHealth 后 `setHealth(getMaxHealth())`，修复原版重生只回 20 点基础血量不回魂环加成部分的问题；换维度克隆不回满（保持原血量）
- 编译通过

**注意事项**：
- 飞行消耗/回复用 `player.tickCount % 20 / % 40` 计数，实体年龄从出生累计，必定周期命中
- 满配参考：99 级 + 9×百万年环 ≈ 1134万血 / 113万攻击力 / 56.8万精神力；空手攻击 1 + 113万，与魂兽量级对齐

---

## 32. 经验系统：魂环吸收升级 + 越级惩罚 + 多人平分 + 连升上限（2026-08-16）

**明细**：
- **经验公式**（新建 `util/SoulExp`，全部集中在常量/公式中可调）：
  - 升级所需经验 `expRequired(level) = 100 × level^1.8`（1→2 级 100，99 级约 39 万；指数高于魂兽经验曲线 0.8，越后期每级所需越多——长期玩法：30 级杀万年约 2.9 只/级、60 级杀十万年约 2 只/级、90 级杀百万年约 1.9 只/级；1→99 累计约 1376 万经验）
  - 魂环基础经验 `baseExp(age) = 10 × 年限^0.8`（与魂兽血攻曲线 statMultiplierOf 同指数；十年≈63 / 百年≈398 / 千年≈2512 / 万年≈15849 / 十万年≈10万 / 百万年≈63万）
  - 越级惩罚 `penalty(playerLevel, age)`：玩家等级档位（level/10，封顶 5=百万年）高于魂兽年限档位时每高一档 ×0.8（30 级杀千年 ×80%、40 级杀千年 ×64%…，防止刷低级魂兽）；不高于时无惩罚
  - 多人平分：每位参与者吸收时获得 基础经验 × 惩罚 ÷ 参与人数（每人各吸一份，最低 1 点）
  - **单次吸收连升上限**：`MAX_LEVELUPS_PER_ABSORB = 5`——一次吸收最多连升 5 级，升满后剩余经验全部作废（金色提示「单次吸收最多连升 5 级，多余 X 经验已消散！」）——防高等级玩家带低等级刷百万年直接直升几十级；未升满 5 级就撞 99 级封顶时剩余经验仍保留
- **吸收交互**（`entity/SoulRingEntity`）：新增 participants/absorbed 两个 UUID 列表（NBT 持久化，读档保留）；override `interact`——蹲着（潜行）右键魂环 → 服务端校验链：激活等级系统 → 是击杀参与者 → 未吸收过 → 未满级（99）→ 计算经验 → `SoulExp.gainExp` 加经验升级 → markAbsorbed → 全员吸收完后 `discard()` 魂环消散；经验拾取音效 + ENCHANT 粒子 + 绿色提示（有惩罚时显示折扣百分比）；未蹲着右键时动作栏黄色提示
- **多玩家击杀记录**（`SoulBeastSpawnHandler`）：`LAST_PLAYER_DAMAGE` 从 `Map<UUID,Long>`（只记最后一人）改为 `Map<UUID, Map<UUID,Long>>`（魂兽→玩家→伤害tick）；LivingDropsEvent 筛选 30 秒窗口内所有伤害过的玩家写入魂环参与者列表
- **经验持久化与同步**：`PlayerLevelData` 新增 exp 字段（CODEC optionalFieldOf("exp",0) 兼容旧档 + STREAM_CODEC writeInt + copyFrom/reset 同步处理 + getExp/setExp/addExp）
- **升级处理**（SoulExp.gainExp）：exp 攒满后连续升级（while 循环扣除每级所需，计数 ≤5），跨 10 级边界获得新魂环（新环默认十年，已有年限的环位不覆盖）；升级重算血量/攻击力并回满血（setLevel 已回满精神力）、授予境界成就、升级音效 + 脚底粒子 + 「修为提升」提示；99 级封顶多余经验保留在经验条内
- 语言文件中英同步 9 个新键（exp_shift_hint / exp_absorb / exp_absorb_penalty / exp_not_participant / exp_absorbed / exp_full / exp_not_activated / exp_capped）
- 构建验证通过（BUILD SUCCESSFUL）

**注意事项**：
- 经验数值平衡全部集中在 SoulExp（expRequired 系数 100 与指数 1.8、baseExp 系数 10、penalty 每档 0.8、MAX_LEVELUPS_PER_ABSORB=5）：想更难升级加大 expRequired 系数或指数，想改惩罚幅度改 0.8
- 魂环参与者只记录击杀时 30 秒窗口内伤害过魂兽的玩家，铁傀儡/环境击杀不生成魂环（无参与者）；参与者离线也不影响，魂环 NBT 持久化等待上线吸收
- 多人平分的语义：每个参与者可各吸收一次，各得 base×自己的惩罚÷人数；参与者等级不同时各自按自己的惩罚系数计算
- 魂环实体 interact 中服务端校验链的顺序就是文案反馈顺序（激活→参与者→已吸收→满级），客户端 interact 直接返回 SUCCESS 只负责摆臂
- 玩家魂环（自身 10 级一环）与经验升级解耦：经验升级跨 10 级自动获得新魂环（默认十年，仅当该环位年限为 0 时写入，调试棒调过的环不覆盖）——后续「魂环吸收玩法」可覆盖年限
- 连升上限的边界：恰好 5 级时剩余经验清零；升 4 级就到 99 级封顶时剩余经验保留（封顶优先级高于上限作废）

---

## 33. 等级节点封顶：吸收魂环获得新魂环（不升级）（2026-08-16）

**明细**：
- 用户需求（修正版）：经验不与原版经验互通；升级经验只能蹲着右键魂环获取；升到 10 级后经验条变成 **0/1**（魂环进度，需要获取魂环）；获取魂环后**还是 10 级**（不升级），经验条恢复；到达节点等级（10/20/.../90）后**溢出的经验值全部作废**
- **节点封顶（到达即封顶，非经验满后封顶）**（SoulExp）：`isBreakpoint(level)` 不变（10~90 且 %10==0）；`isBottleneck(data)` 改为「节点等级且尚未获得对应魂环（已获环数 < 等级/10）」，与经验无关；gainExp：封顶中吸收经验全部作废（金色提示）；升级 while 循环中**升到节点等级立即 break**，溢出经验全部清零作废（金色提示「升至 X 级瓶颈，溢出 Y 经验已作废！」）；循环结束若处于瓶颈再发瓶颈提示
- **吸收魂环获得新魂环**（SoulExp.absorbRing 替代 tryBreakthrough + SoulRingEntity.interact）：interact 校验链（激活→参与者→已吸收→满级）后瓶颈分支——`isBottleneck` 时吸收魂环实体直接获得新魂环（默认十年，已有年限环位不覆盖）——**不升级、不消耗经验、不结算魂环实体经验**；授予境界成就 + 血攻重算回满 + 金色提示「获得第 X 魂环，晋升为 X！」；封顶解除后经验条恢复，继续攒经验升级；非瓶颈时仍走经验吸收分支；吸收音效粒子 playAbsorbEffects 共用
- **环数语义全局改为已获环数（与等级解耦）**：`getRingCount()` 改为数 ringAges 中**年限大于 0 的环位数量**（节点封顶未获取新环不计入，吸收魂环获得新环后 +1，与等级无关）；SoulRingRenderer（渲染环数）、SoulGrowth 三处（血/攻/精神力加成）、TiaoShiBangItem（移除/开武魂/年限调节/获取）、DebugStickInputHandler（环大小调节）全部自动适配；SoulRingRenderer 注释同步更新
- **成就按获得魂环授予**（ModAdvancements，不变）：awardRing(player, ringCount)（第1环=魂师 rank_2 … 第9环=封号斗罗 rank_10）；absorbRing 与调试棒获得环时调用；95/99 等级成就由 gainExp/调试棒升级后 awardSoulRank
- **称号改为按已获魂环数计算**（TitleSystem）：getTitle 签名改 `(level, ringCount, customTitle)`——0 环=魂士（tier_1）… 8 环=魂斗罗（tier_9）；**9 环即封号斗罗**（90 级获得第 9 环即晋升，91 级起可自定义封号）；95 超级/99 极限按等级覆盖；调用点全部传 data.getRingCount()（LevelHudOverlay、主类 /fh 两处、SoulExp.absorbRing）
- **调试棒适配**：grantSoulRing 改为「+10 级并按新等级补齐对应魂环」——needed = min(新等级/10, 9)，从 ringCountBefore 环位起逐个补十年环并授予成就（+10 跨过一个节点补一环，跳过节点时不漏环）；removeSoulRing 改为**显式清除最外层环年限**（setRingAge(ringCountBefore-1, 0)）+ 等级 -10（新语义下等级下降不再自动减环）
- **HUD 经验条 0/1**（LevelHudOverlay）：瓶颈封顶时经验行显示金色「0/1」（魂环进度），经验条位置显示金色提示文字「需要获取魂环！」；非封顶时正常显示经验值与绿色进度条
- 语言文件中英同步：exp_bottleneck / exp_breakthrough 文案更新，新增 exp_wasted / hud.need_ring；编译验证通过

**注意事项**：
- **与条目 32 的关系**：条目 32 的「升级跨 10 级自动获得新魂环」已被本条取代（新环一律由节点封顶后吸收魂环获得）；连升 5 级上限与节点封顶可同时触发（如 5 级升到 10 级恰好 5 级），此时溢出经验按封顶作废文案提示（exp 已清零，exp_capped 不再重复触发）
- 旧存档迁移：改造前 10 级自动有 1 环（index 0 = 10 年）的玩家，getRingCount()=1 → 不封顶可直接继续升级，平滑过渡；改造前突破到 11 级 1 环的玩家同样不封顶，影响可忽略
- 封顶期间吸收魂环实体的经验不结算（获得新魂环语义优先）；多人场景各参与者按各自状态分别走获取魂环/经验分支
- 调试棒是调试工具：直接 +10 级并补齐缺失魂环（模拟吸收魂环突破），与正式吸收互不影响
- 90 级 9 环后不再有节点（91~99 正常升级）；99 级满级封顶不变

---

## 34. 修复：魂环实体右键无法命中 + 客户端同步范围过小（2026-08-16）

**明细**：
- 现象 1：蹲着右键掉落的魂环无任何反应（interact 从未被触发，日志无任何提示）
- 现象 2：击杀魂兽后「魂环不掉落」（实体实际已生成并存档，但客户端看不到）
- 原因 1：Entity 基类 `isPickable()` 默认 false，客户端准星命中判定直接跳过魂环实体，右键永远打不到它
- 修复 1（SoulRingEntity）：覆写 `isPickable()` 返回 true；覆写 `getPickRadius()` 返回 0.6F（只影响准星命中 inflate 判定，不改碰撞箱/物理/渲染，改善默认 0.5×0.1 扁碰撞箱的瞄准手感）
- 教训：曾尝试覆写 `makeBoundingBox()` 返回 1.2×1.0×1.2 碰撞箱，实测导致魂环客户端不可见（渲染回归），已回退；对已有正常渲染的实体，不要动碰撞箱，右键手感用 getPickRadius 解决
- 原因 2：`clientTrackingRange(8)` 同步范围仅 8 格，玩家击杀后后退几步魂环就超出同步范围从客户端消失（击杀时同步、远离即卸载，造成「不掉落」假象；日志验证：服务端生成与存档正常，客户端靠近时能构造实体）
- 修复 2（ModEntities）：`clientTrackingRange(128)`（8 chunk 距离，魂环 synchedData 仅一个 int，同步开销可忽略）
- 调试手段：SoulBeastSpawnHandler.onLivingDrops 加 `[SoulRing-drop]` 日志（无击杀参与者/超时窗口/生成成功三处）+ SoulRingEntity 构造/移除日志（区分 side），定位「生成→同步→渲染」链路
- 编译验证通过

**注意事项**：
- 原版 Entity 子类要支持右键交互必须同时满足：`isPickable()` 返回 true + 非零碰撞箱（EntityType.Builder.sized 已提供默认碰撞箱，无需覆写 makeBoundingBox）
- 对已有正常渲染的实体改碰撞箱（makeBoundingBox）可能引发客户端渲染回归；右键瞄准手感应通过 getPickRadius 调节（仅作用于准星命中判定）

---

## 35. 魂环吸收确认弹窗：怪物种类/年限/技能/吸收概率（2026-08-16）

**明细**：
- 用户需求：不蹲右键魂环弹出提示窗（查看信息），显示怪物种类、年限、技能（目前没有，显示开发中）、吸收概率（开发中），确认后才吸收；蹲着右键直接吸收为经验（原行为）
- **魂环实体记录怪物种类**（SoulRingEntity）：新增 `DATA_SOURCE_TYPE`（String synchedData，存怪物类型本地化键如 entity.minecraft.slime）+ `getSourceType()/setSourceType()` + NBT 持久化 SourceType；SoulBeastSpawnHandler.onLivingDrops 生成魂环时 `setSourceType(entity.getType().getDescriptionId())`；旧存档无该字段 → 窗口显示「未知魂兽」
- **交互流程重构（两阶段确认 + 直接吸收双通道）**：interact 改为——蹲着右键直接调 `performAbsorb`（原校验链与吸收行为不变，失败红字提示）；不蹲右键 `PacketDistributor.sendToPlayer(OpenRingAbsorbScreenPayload(getId()))` 发窗口包并返回 CONSUME；原吸收逻辑（瓶颈获取魂环分支 + 经验计算/加经验/标记/消散/音效粒子/提示/同步）整体抽为 `performAbsorb(ServerPlayer)`，返回 boolean，内置完整校验（激活/参与者/已吸收/满级，失败提示）防伪造确认包与蹲吸失败提示
- **新网络包**（network）：`OpenRingAbsorbScreenPayload`（服务端→客户端，携魂环实体 ID，打开窗口）+ `ConfirmRingAbsorbPayload`（客户端→服务端，携魂环实体 ID，重查找实体后调 performAbsorb）；ModNetwork 注册（playToClient / playToServer）
- **吸收确认窗口**（新建 `client/SoulRingAbsorbScreen`）：标题「魂环吸收」+ 四行居中信息（怪物种类＝translatable(魂环 SourceType 本地化)、年限＝SoulBeastAge.format、技能：开发中、吸收概率：开发中）+ 两个按钮「吸收」（发确认包后关闭）/「取消」；信息从客户端世界实体 synchedData 读取；`isPauseScreen()` 返回 false（窗口打开不暂停游戏）；ESC 关闭
- 语言文件中英同步 9 个新键（screen.ring_absorb.title/beast/age/skill/probability/developing/confirm/cancel/unknown_beast）；编译验证通过

**注意事项**：
- 窗口信息依赖客户端实体同步数据（synchedData），实体卸载/消散时窗口显示兜底文案「未知魂兽/？」；确认时服务端重新查找实体并完整重校验（状态可能已变化，失败时红字提示）
- 怪物种类用 EntityType.getDescriptionId()（原版本地化键），客户端 translatable 自动按客户端语言显示；跨 mod 生物同样适用（有注册名即有键）
- 技能与吸收概率为占位文案「开发中」，后续实装时在 SoulRingAbsorbScreen.render 替换对应行 + 必要时在魂环实体加新同步字段

---

## 36. 修复：调试棒面板查看被动生物崩溃（2026-08-16）

**明细**：
- 现象：手持调试棒查看牛/羊/鸡等被动生物时游戏崩溃（`IllegalArgumentException: Can't find attribute minecraft:generic.attack_damage`，SoulBeastInspectorOverlay.render L76）
- 根因：被动生物无 ATTACK_DAMAGE 属性，面板渲染时无防御直接 `getAttributeValue(ATTACK_DAMAGE)`；魂兽化处理器（SoulBeastSpawnHandler L130/201）已用 null 检查防御，但查看面板漏了
- 修复：SoulBeastInspectorOverlay 攻击力行改为先 `getAttributes().hasAttribute(ATTACK_DAMAGE)` 再取值，无攻击属性显示 N/A

**注意事项**：
- 读取非保底属性（attack_damage 等）前必须 hasAttribute/null 检查；末影龙也无 ATTACK_DAMAGE 属性（伤害走特殊机制）；被动生物攻击显示 N/A 与魂兽化日志 attack=N/A 一致

---

## 37. 调试棒新增增减等级模式 + 模组版本/作者（2026-08-16）

**明细**：
- 用户需求：调试棒新增增减等级功能；模组版本设定为 v0.1-beta、作者为梵阿景
- **增减等级模式**（`DebugStickMode.LEVEL("level")`）：右键等级 +1（回满血，升级音效），Alt+右键等级 -1；直接改等级数值不增减魂环，1~99 内越界提示；`TiaoShiBangItem.adjustLevel(Level, Player, delta)` 静态方法：clamp 校验 → setLevel（内置精神力刷新）→ SoulGrowth.applyHealth/applyAttack → 升级回满血 → 同步客户端 + 音效 + 提示
- **右键加减级链路**：Alt 是客户端按键状态，服务端 use 无法感知 → `DebugStickInputHandler.onClientTick` 中增减等级模式 + `mc.options.keyUse.consumeClick()` → 发 `AdjustLevelPayload(Screen.hasAltDown() ? -1 : 1)`（C2S，右键统一客户端捕获按 Alt 分流；服务端 use 的 LEVEL 分支无功能）；服务端校验手持调试棒 + 模式为 LEVEL + delta 为 ±1 后执行
- **版本与作者**：gradle.properties `mod_version=0.1-beta`（原 1.0-SNAPSHOT）、`mod_authors=\u68b5\u963f\u666f`（Unicode 转义写法防 properties 编码乱码）；neoforge.mods.toml 模板经 processResources expand 生成 version="0.1-beta"、authors="梵阿景"，已验证生成结果正确（终端 GBK 显示乱码是 PowerShell 解码问题，文件内容实际正确）
- 语言文件中英同步 4 个新键（mode.level / level_up / level_down / level_limit）
- **调试棒悬浮介绍**：`TiaoShiBangItem.appendHoverText` 列出全部 8 个模式的操作说明（含潜行+滚轮切换提示与魂兽属性面板提示），新增 11 个 tooltip.* 键（中英同步）；模式行按 DebugStickMode.values() 顺序动态生成
- **打包导出 v0.1-beta（2026-08-16）**：`gradlew build` 产出 build/libs/all_spirit_continent-0.1-beta.jar（约 11 MB），已复制到桌面；jar 已验证：含 all_spirit_continent.mixins.json（已删除 refmap 字段，见注意事项）与 META-INF/neoforge.mods.toml（version="0.1-beta"、authors="梵阿景"、[[mixins]] 声明齐全）

**注意事项**：
- 增减等级不增减魂环、不触发成就（纯调试工具，与 SOUL_RING 获取魂环模式区分）；等级与环数解耦（第 33 条），不补环不会导致渲染异常，但节点级封顶机制由经验吸收路径检查，直接改等级不受影响
- 右键加减级统一走网络包服务端重校验（防伪造，delta 限 ±1）；Alt+右键不会触发其他副作用（use 的 LEVEL 分支无功能）
- gradle.properties 中文值用 \uXXXX 转义（java.util.Properties 规范），直接写中文可能因 ISO-8859-1 解码乱码
- **Mixin 发布无需 refmap**：NeoForge 21.1（1.20.4 起）生产环境以 Mojang 官方映射名运行，mod 字节码与 Mixin 目标名编译时就是官方名，运行时一致无需重映射；ModDevGradle 2.0.142 未自动启用 Mixin AP（compileJava 无注解处理器、无 refmap 生成属正常），mixins.json 中的 refmap 字段已删除防运行时告警；打包前清理了 javap 调查临时文件（tmp_javap*.txt）

---

## 38. 修复吸收魂环白环 + 调试棒新增「查看魂环年限」模式（2026-08-16）

**明细**：
- **修复：吸收魂环获得的新魂环一律白环（十年）**：用户反馈吸收任何年限的魂环，获得的都是十年白环。根因是 `SoulExp.absorbRing` 硬编码 `setRingAge(ringIndex, 10)`（第 33 条时期吸收玩法未实装的占位）。改为：`absorbRing(Level, ServerPlayer, int ringAge)` 新增年限参数，新环年限继承吸收的魂环实体年限（`SoulRingEntity.performAbsorb` 传入 `getRingAge()`），年限缺失（≤0）才兜底十年——**魂兽是多少年，吸的魂环就是多少年**；突破提示文案 exp_breakthrough 加年限参数（如「获得第 1 魂环（1.2万年），晋升为魂师！」）
- **调试棒新增《查看魂环年限》模式**：`DebugStickMode` 新增第 9 个枚举 `CHECK_RING_AGE("check_ring_age")`，Shift+滚轮可切入；`TiaoShiBangItem` 新增 `checkRingAges()`：右键在聊天栏一条多行消息（标题 + 9 行，`Component.literal("\n")` 拼接）列出自己 9 个魂环的年限，每环一行按年限档位上色（复用新增的 `SoulBeastAge.tierFormatting(age)`：十年白/百年黄/千年浅紫/万年灰/十万年红/百万年金，与 tierColor 语义一致），没有魂环的环位灰色显示「无」；tooltip 按 DebugStickMode.values() 动态生成自动包含新模式
- 语言文件中英同步：mode/tooltip/msg 共 5 个新键 + exp_breakthrough 文案更新
- 编译验证通过（BUILD SUCCESSFUL）

**注意事项**：
- 已有年限的环位仍不覆盖（`getRingAge(ringIndex) <= 0` 才写入），瓶颈吸收场景该环位必然为空，不影响正常流程；调试棒《获取魂环》模式补的十年环属调试工具，正式玩法的环年限一律来自吸收的魂环实体
- 聊天栏多行消息用 `MutableComponent.append(Component.literal("\n"))` 拼接，一条 sendSystemMessage 即完整列表；换行符在 Component 中可正常渲染
- `SoulBeastAge.tierFormatting` 与 `tierColor` 两套颜色体系分别服务聊天文本（ChatFormatting）与渲染文字（ARGB int），新增档位时两处同步维护

---

## 39. 年限显示改为完整年数（去掉 x.x万年 缩写）（2026-08-17）

**明细**：
- 用户反馈：魂兽年限不要显示「x.x万年」缩写，吸收的魂环也要显示完整年限
- `SoulBeastAge.format` 改为**始终返回完整年数**（如 6070年、42813年、99990000年），删除整数万「X万年」与小数「X.X万年」两种缩写分支
- 全部年限显示入口自动生效（均复用 format）：魂兽头顶命名牌、魂环实体头顶命名牌、魂兽属性查看面板、魂环吸收确认窗口、吸收突破提示（exp_breakthrough）、调试棒年限修改提示（ring_age）、调试棒查看魂环年限列表、魂环物品 tooltip
- 编译验证通过；重新导出 jar 至桌面（0:36 构建，已覆盖旧版）

**注意事项**：
- 年限显示格式只改 `SoulBeastAge.format` 一处，未来新功能显示年限一律复用此方法，不要自行拼「X万年」
- HUD 血量/精神力大数值（LevelHudOverlay 的「X.X万」）与经验显示（SoulExp.formatExp）不在本次需求范围，保持原样
- 9999万年（99_990_000）头顶命名牌会显示「99990000年」，较长属预期（用户要求完整显示）

---

## 40. 蹲着右键魂环只逸散经验：不直接获得新魂环（2026-08-17）

**明细**：
- 用户反馈：即使已经到瓶颈可以吸收魂环，蹲着右键也不能直接把魂环吸到自己身上，应直接当作经验逸散掉
- `SoulRingEntity` 吸收逻辑拆分：`performAbsorb(ServerPlayer)` 拆为带标志的重载 `performAbsorb(ServerPlayer, boolean allowRingGain)`
  - **蹲着右键**：`interact` 传 `allowRingGain=false`——跳过⑤瓶颈获取魂环分支，一律走经验吸收；瓶颈封顶时 `SoulExp.gainExp` 经验全部作废，魂环消散逸散，灰色提示「你处于瓶颈封顶，魂环经验逸散了！」（新增 exp_dissipated 语言键，中英同步）
  - **不蹲右键弹窗确认「吸收」按钮**（ConfirmRingAbsorbPayload 路径）：传 `allowRingGain=true`——瓶颈时仍可获得新魂环（年限继承魂兽年限），非瓶颈照常吸收经验；获得新魂环的唯一入口
- 吸收提示⑩改为三分支：瓶颈逸散（灰）/ 越级惩罚折扣（绿）/ 正常获得经验（绿）
- 编译验证通过；重新导出 jar 至桌面（0:49 构建）

**注意事项**：
- 蹲着右键瓶颈逸散时 `gainExp` 会先发金色 exp_bottleneck 瓶颈提示（指导如何突破），再发灰色 exp_dissipated 逸散提示（本次结果），两条顺序自然
- 确认窗口「吸收」是瓶颈突破唯一正式入口（调试棒获取魂环模式属调试工具）；防误操作语义：蹲吸=快速刷经验，点确认=认真突破
- exp_shift_hint 文案「蹲下（潜行）右键魂环可吸收为经验」语义仍准确（逸散也是经验结算的一种），暂未改

---

## 当前进度（2026-08-17）

- ✅ 魂兽系统全部完成：原版生物魂兽化（年限/体型/攻击力/生命值【血攻共用 statMultiplierOf=年限^0.8：1年原版值 → 9999万年血量约×250万（僵尸≈5000万）、攻击同倍数保持原版血攻比例，魂兽化后回满血；已解除原版属性上限（max_health 1024/attack_damage 2048）并开启攻击力客户端同步】/脚底魂环/头顶「名称+年限」命名牌 + 血量第二行）、百万年限制与冷却重生、新手保护、Boss 百万年、击杀校验、魂环实体掉落、双配置文件（死亡开关 + 自定义魂环年限表）、调试棒属性查看面板（攻击力/血量/体型/年限，手持调试棒或「魂视」附魔头盔触发，被动生物攻击力显示 N/A 已修复崩溃，第 36 条）、魂视附魔（头盔专属）、成就页「全魂大陆」（《觉醒武魂？！》获得水晶球 + 分支1境界成就链：魂士=使用完水晶球、魂师~魂斗罗=每环一个、封号斗罗=第九环、超级斗罗=95级、极限斗罗=99级，随获得魂环/修为提升自动授予；调试棒 90 级后改为每次 +1 修为；成就图标用魂环年限贴图：魂士=水晶球、魂师十年→大魂师百年→魂尊千年→魂宗万年→魂王十万年→魂帝起百万年）、玩家成长体系（血量/精神力/攻击力随等级与魂环年限提升 + 魂帝境界精神力飞行 + 重生回满血，详见第 29~31 条）、**经验系统（第 32 条：1-99 级经验升级【expRequired=100×等级^1.8 长期曲线】，蹲着右键魂环吸收经验，年限越高经验越多，必须参与击杀才能吸收，多人参与平分，越级击杀低级魂兽经验惩罚 ×0.8/档，单次吸收最多连升 5 级超出的经验作废）、等级节点封顶（第 33 条：10/20/.../90 级到达即封顶、溢出经验全部作废、经验条变 0/1；吸收魂环获得新魂环【第 38 条：年限继承魂兽年限，不再默认十年白环】后经验条恢复；称号改为按已获魂环数计算【0 环魂士…9 环封号斗罗】，环数全局改为实际持有环数（ringAges 非零计数，与等级解耦））、魂环吸收确认弹窗（第 35 条：不蹲右键魂环弹出确认窗口【怪物种类/年限/技能开发中/吸收概率开发中】，「吸收」按钮确认后由服务端执行吸收【瓶颈时获得新魂环，第 40 条：唯一正式突破入口】，「取消」/ESC 关闭；蹲着右键直接逸散为经验【第 40 条：瓶颈封顶时经验作废、不直接获得新魂环】）、**调试棒增减等级模式（第 37 条：右键+1回满血、Alt+右键-1，1~99，不增减魂环；悬浮介绍 tooltip 列出全部模式操作说明）、**调试棒查看魂环年限模式（第 38 条：右键在聊天栏列出自己 9 个魂环的年限，每环按档位上色，无魂环显示「无」）、模组版本 v0.1-beta、作者梵阿景、**打包导出 v0.1-beta 至桌面（含 mixin 配置，无需 refmap）**
- ⏳ 待游戏内实测/可调项：概率权重手感（SoulBeastAge 各表）、体型曲线（scaleOf）、血攻倍数指数（statMultiplierOf 指数 0.8）、百万年补刷距离与冷却时长、魂兽环半径公式、Boss 百万年体验、自定义配置模板使用体验、血量第二行与调试棒查看面板的视觉效果、攻击力加成手感（RING_BASE_ATTACK=2）、飞行消耗/回复节奏（SoulFlight 常量）、**经验数值平衡（SoulExp 三个公式）**、**魂环吸收弹窗视觉效果与按钮手感**
- 📋 后期规划：魂环吸收玩法实装（技能/吸收概率机制，吸收窗口第 35 条已就绪）、魂兽专属实体与模型、魂兽区生成结构、其他 mod 生物配置示例补充、玩家侧武魂/魂环属性成长体系（匹配高数值魂兽）

